import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="หน้าหลัก"
      note="สรุปค่าความดันล่าสุดและการ์ดสถานะ"
      portedFrom="client-old/app/(tabs)/index.tsx"
    />
  );
}
