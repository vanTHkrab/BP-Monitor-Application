import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HistoryScreen() {
  return (
    <ScreenPlaceholder
      title="ประวัติ"
      note="กราฟแนวโน้มและรายการค่าที่บันทึกไว้"
      portedFrom="client-old/app/(tabs)/history.tsx"
    />
  );
}
