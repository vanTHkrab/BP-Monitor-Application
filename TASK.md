# BP Monitor — Task Board

_Last updated: 2026-08-07 · Updated by bp-task_

## Imports

<!-- bp-task resolves these at read time. Tasks from imported files are merged
     into the board with their scope prefix. Duplicates are skipped. -->
@docs/project/api-gateway-plan.md

---

## Tasks

### client

- [ ] **C-001** `high` Wire caregiver push-notification preference screen to the store
- [ ] **C-002** `medium` Add 7-day BP trend chart to the history tab
- [ ] **C-003** `medium` Implement PIN / biometric lock for the app-open flow
- [x] **C-004** `high` Integrate on-device YOLO pre-flight result into camera UI warning banner — closed 2026-08-06 as superseded, not implemented: the live framing gate replaced the warning-banner design and already shipped (`docs/project/CLIENT-remaining.md` §10)
- [ ] **C-005** `medium` Restore the caregiver "ดูข้อมูล" jump from invitations into a patient's readings — ship with the home/history tab port; `client/src/app/invitations.tsx` should set the viewing context in `client/src/modules/caregivers/`, not the auth store (see `docs/project/CLIENT-auth-structure.md`, "activePatientId")
- [ ] **C-006** `medium` Stop `formatErrorMessage` leaking English into user-facing errors — `client/src/services/api.ts:107` prepends `"<OperationName> failed: "`, and `stripCode` in `client/src/lib/error-message.ts:23` is anchored (`/^\s*\[[A-Z_]+\]\s*/`) so it never strips the code; affects all six call sites (post, invitations ×3, post/[id] ×2, readings, community) — preferred fix is dropping the operation-name prefix, since it is developer data
- [x] **C-007** `medium` Add a severity filter to reading history (FR-03.2) — done 2026-08-07, branch `client/history-severity-filter`, commit `84f427ca`, open as **PR #96**. Four pills that **partition** `BPStatus` rather than overlap: ทุกระดับ · ปกติ (`normal`) · เฝ้าระวัง (`low`, `elevated`) · สูง/สูงมาก (`high`, `critical`). `low` sits with เฝ้าระวัง because a taxonomy organised around the word "high" silently orphans hypotension; the partition is asserted against the exported `BP_STATUSES`, so a sixth status fails a test rather than landing in no group. **The severity filter scopes the list, not the chart** — the chart is fed from the time-filtered array only, because a trend line through only the critical points hides every normal reading between them and a mostly-fine patient would read as being in continuous crisis. That asymmetry is deliberate; do not "fix" it into consistency. `history-list.tsx` got severity but not period, and the group carries over as a **parsed** route param falling back to "everything", so a hand-edited deep link cannot render an empty list — that screen had no test file at all before. **The daily time bucket was considered and rejected, not overlooked:** a fifth time pill while a second filter row competes for the same vertical budget is the wrong trade against a 44dp tap target for an elderly-first audience; the reasoning is written up in `docs/project/CLIENT-history.md`. 56 suites / 713 tests (from 54 / 659); GraphQL operations held at 50, which was a requirement — the filter is local, so any movement would have meant the screen started asking the server to filter
- [x] **C-008** `low` Delete the stale comment at `client/src/app/patient-health.tsx:39-41` — done 2026-08-07, same branch as C-007. It claimed `myPatients` returns neither `gender` nor `congenitalDisease` and that those two fields therefore start blank with a note explaining why; commit `e15b1118` had added both to `PatientSummaryType` and deleted that note. The comment is now rewritten rather than merely removed: it says all five fields seed, and explains that the submit is still a **patch** for a different reason — two caregivers can look after the same patient, so a full-form write would let the second to submit silently revert a field the first had just changed

### web

- [ ] **W-001** `high` Build clinician dashboard: reading list with BP-status filter
- [ ] **W-002** `medium` Add patient search and profile page
- [ ] **W-003** `low` Export readings as CSV from the web dashboard

### api-gateway

- [ ] **A-001** `high` Add pagination to `readings` GraphQL query
- [ ] **A-002** `medium` Rate-limit the `uploadBPImage` mutation per user
- [ ] **A-003** `low` Expose a `healthz` endpoint for the infra liveness probe
- [x] **A-004** `medium` Fix `addCaregiverPatient`'s unusable default relationship — already done in the code when this was audited on 2026-08-06: `VALID_RELATIONSHIPS` in `caregiver.service.ts` contains `caregiver`, with a comment recording exactly this bug (the schema default failed its own check and every invite relying on it was silently stored as `other`). The set was widened rather than the default changed, so existing clients keep working; `patient` stays out, because the column describes the caregiver's relationship _to_ the patient. Closed as already-implemented, not newly fixed
- [x] **A-005** `medium` Let a caregiver find a patient by email as well as phone in `addCaregiverPatient` — done 2026-08-06, both sides: the mutation argument is now the polymorphic `patientContact` (contains `@` ⇒ email, else phone; no `patientPhone` alias — the `CaregiverLinkType.patientPhone` _field_ is unchanged), and the client sends it from one untoggled input backed by `client/src/modules/caregivers/lib/contact.ts`
- [x] **A-006** `medium` Normalise `email` in `AuthService.updateProfile` — done 2026-08-06. `data.email` is now trimmed and lowercased **once, before the uniqueness check** (`server/app/api-gateway/src/auth/auth.service.ts:290`) and the normalised value is what gets patched (`:309`). Normalising at the top rather than only at the write fixes a second instance of the same bug that the original entry did not name: the pre-flight `findUnique({ where: { email } })` is exact-match on a `@unique` column, so a caller submitting `Foo@x.com` was checking a different key than the one being written — it could miss a real conflict and let the update fail at the database constraint instead of returning the intended `อีเมลนี้ถูกใช้งานแล้ว`. Still 0 bad rows, so no backfill was needed; this closes the only write path that could create one
- [x] **A-007** `high` Rate-limit `addCaregiverPatient` — done 2026-08-06: 10 attempts / 10 min, keyed on the **caregiver's user id** (keying on the contact string would let an attacker rotate addresses and never spend a budget) and counting every attempt, found or not. The honest `ไม่พบผู้ใช้จากอีเมลนี้` stays, as decided in A-005; this is its mitigation. The INCR + PEXPIRE Lua that used to live in `login-throttle.guard.ts` now lives in `server/app/api-gateway/src/redis/rate-limit.service.ts` — extracted out of `auth/better-auth.ts`, where Better Auth had inherited it, and covered by tests first. Refusal is a 429 carrying `retryAfterSec`, so the mobile countdown works unchanged. **Known and accepted:** the window is fixed, so 10 at 09:59 + 10 at 10:01 is 20 in two minutes; a sliding window would change login-throttle behaviour through the shared primitive — see A-008
- [x] **A-008** `low` Decide whether the shared rate limiter should slide instead of jump — **decided 2026-08-06: keep the fixed window, keep the budgets. Nothing was built; a trade-off was chosen.** The window starts on the first hit, not on a clock boundary, so a caller who spends the full budget at T and again at T+window gets 2x `max` back to back (Better Auth's credential routes 5/15min ⇒ a momentary 10; `addCaregiverPatient` 10/10min ⇒ a momentary 20). Accepted because the **sustained** rate is unchanged — only the momentary burst doubles — and both threats this limiter answers (credential stuffing, and the account enumeration A-007 mitigates) are priced in attempts per _hour_, not per second, so 2x for one instant barely moves an attacker's cost. Halving `max` was considered and **rejected, not overlooked**: three login attempts per fifteen minutes is not enough for a real user mistyping a password, so it degrades legitimate users to fix something that does not meaningfully help an attacker. A sliding window (sorted-set log, or two-bucket weighted counter) closes it precisely, but every consumer shares this one primitive — changing it changes Better Auth's credential routes too, and `consumeInMemory` would have to implement the same algorithm or a Redis outage would silently change semantics: large surface, small gain. **Revisit if:** a consumer needs a budget large enough that halving it is expensive; `retryAfter` needs to be second-accurate for UX; or a compliance rule names sliding windows. Full rationale lives as a doc comment above the `CONSUME` script in `server/app/api-gateway/src/redis/rate-limit.service.ts` — do not reopen this from first principles
- [x] **A-009** `high` Deliver push notifications for critical BP readings (FR-04.1) — **gateway half done 2026-08-07**; the client half is tracked separately as **C-001**. `PushToken` (`prisma/migrations/20260806164542_add_push_tokens`) is one row per app *installation*, keyed on a globally-`@unique` Expo token rather than on the user: the token belongs to the device, so a shared handset **reassigns** the row on the next `registerPushToken` instead of accumulating one per account — two rows would mean a patient's critical reading reaching whoever used the phone last. It deliberately does not hang off `user_sessions`, because a token must survive session rotation or a caregiver silently stops receiving alerts the moment their session refreshes; the price is that logout deletes it explicitly, which `logout(pushToken:)` / `logoutAllDevices(pushToken:)` now do (both arguments optional and additive — old call sites still compile). `ReadingService.createAlertForReading` sends via `PushService.notifyUsers` at `reading.service.ts:216`, fire-and-forget, reusing `getCaregiverAlertMessage` verbatim so there is one set of Thai copy rather than two. **Critical only, caregivers only** — both are decisions, not omissions: `Alert` rows are still written for `warning`, but a caregiver pushed for every warning-level reading of a chronic-hypertension patient mutes the channel and then misses the one that mattered; and the patient is holding the phone that just took the measurement. Dead tokens are pruned from both the send ticket and, 30 minutes later, the delivery receipt (`pending_receipt_id` + `@Cron` sweep) — receipt-stage `DeviceNotRegistered` is the common uninstall case, so ticket-only pruning would let the table grow forever. **Known and accepted:** the sweep's `@Cron` fires on every pod, which is safe here only because every step is idempotent (a receipt fetch is a read; the delete and the clear are already at their target state after the first run) — a *new* cron in this module has to make that argument or take a Redis lock. `EXPO_ACCESS_TOKEN` is optional and unset means Expo accepts a send from anyone holding one of our push tokens; set it in production. **Not exercisable in Expo Go** — Android dropped remote push in SDK 53, so the client half needs a dev build; the gateway treats "no token registered" as an ordinary outcome rather than an error precisely so that degrades honestly
<!-- FORMATTING TRAP — read before "tidying" any multi-line entry below.
     `web/src/lib/tasks.ts` parses this file. A task's note continues only
     while lines match /^\s+\S/ (:121) — indented, and CONTIGUOUS. A single
     blank line inside an entry hits `if (!raw.trim()) current = null` (:129)
     and every line after it is orphaned onto no task at all. A-011 spans the
     whole block below with no blank line in it, on purpose; adding one for
     readability silently deletes most of the board's most severe entry.
     Related: the priority must be exactly `high`, `medium`, or `low` (:52).
     A non-matching value — `critical`, say — does not merely fail to render:
     the line falls through to the continuation branch and is swallowed into
     the PREVIOUS task's note, corrupting a neighbouring entry too. -->
- [ ] **A-011** `high` Compare `NODE_ENV` through one shared helper instead of an exact string match — **six non-spec sites, one change.** A deploy that sets `NODE_ENV=PRODUCTION` or `Production` — a plausible typo, not an exotic one — flips every one of them into its development branch at once. Enumerated so nobody has to re-grep, all verified by reading the body, not the grep line:
  1. `src/debug/debug.service.ts:27` (`=== 'production'`) — serves `debugMyStorage`, a per-user diff of Postgres rows cross-checked against S3 HEAD probes. See **A-012**.
  2. `src/app.module.ts:63` (`!== 'production'`) — **one gate deeper than an earlier draft of this entry claimed, and the correction matters:** this is inside `resolveGraphiqlEnabled`'s `value === ''` arm, reached only when `GRAPHIQL_ENABLED` is unset, empty, or whitespace. An explicit `1`/`true`/`yes`/`on` never consults `NODE_ENV` at all, and `app.module.spec.ts:404-410` already pins that. The exposure is still real — the committed `.env.example:12` ships `GRAPHIQL_ENABLED=` empty, so the unset arm **is** the default path — and it still ends in mutation-capable GraphiQL against live patient data, but it takes a second condition to get there. An over-scoped claim inside the entry that exists _because_ a finding was under-scoped would discredit the entry's own argument, so it is stated at its true width.
  3. `src/app.module.ts:135` (`!== 'production'`) — puts `validationErrors` into `extensions`, echoing raw submitted input back to the client.
  4. `src/main.ts:29` (`!== 'production'`) — calls `app.enableCors()` with **no allowlist**, so any origin may call the gateway. Auth is a bearer token rather than a cookie, so this is not CSRF; it is any page in any tab being able to use a token it has obtained.
  5. `src/auth/better-auth.ts:41` — `isDevelopment()`, used at **three** call sites, `:86`, `:537`, `:555`, which are **two kinds** of guard (the count is a deliberate grouping, not a miscount — say "three sites, two kinds" and both readings are true). All three **invert**: a missing `BETTER_AUTH_URL` stops refusing to boot (`:86`), and a missing delivery provider stops throwing for email (`:537`) and for SMS (`:555`). Worse than "mail goes to a stub" — `:537` then takes its `logger.debug` branch, and its own comment says the body carries a reset link or verification code, "a live credential — so it must never reach a production log". So this site does not merely fail to deliver auth mail; it writes credentials into production logs.
  6. `src/auth/better-auth.controller.ts:34` (`!== 'production'`) — debug-logs 404 paths that the comment beside it says carry tokens.
  **Severity is critical-grade; the label reads `high` for a mechanical reason, not a judgement.** On the evidence this was re-decided upward from `high`: the original `high` rested on the exposure being conditional on a misconfiguration nobody has observed, an argument that quietly assumed we would find out. Site 5 is what breaks it — the guards that exist precisely to refuse a misconfigured boot are themselves among what inverts, so such a deploy comes up green, sends no verification or reset mail, and logs live credentials, with nothing surfacing the cause. A conditional exposure that also disables its own detection is not the same risk as one that announces itself, and the aggregate is no longer one debug query plus a schema explorer. The label stays `high` because `web/src/lib/tasks.ts:52` matches `` `(high|medium|low)` `` only: writing `` `critical` `` silently drops the whole entry from the rendered board — the same failure that once lost the entire `ai-service` scope. **Treat this as the top item in `api-gateway` regardless of the backtick.** The contradiction has two halves in two files, and a follow-up needs both: `.claude/agents/bp-task.md:87` (`**Priority:** critical · high · medium · low`) and again at `:109` permit `critical`, while `web/src/lib/tasks.ts:52` rejects it. Not `TASK.md` itself — its only format comment is at `:75` and enumerates no values. Reconciling them (widen the parser, or drop `critical` from the agent definition) is a separate change, in files outside this one, and is deliberately not made here.
  **Fix all six behind one helper.** A fix scoped to the first two — the shape this entry originally described — closes two of six and leaves unrestricted CORS and the inverted fail-fast guards live behind a closed ticket. (An earlier draft justified the shared helper with "fixing either alone leaves the identical shape in the file next door"; that sentence was wrong on its own facts, since site 3 is 72 lines below site 2 in the _same_ file. The argument holds a fortiori and is restated above.) PR #102 is a worked example of the failure mode: it widened `GRAPHIQL_ENABLED`'s **value** parsing to a closed allowlist (`1`/`true`/`yes`/`on`) and never touched the `NODE_ENV` comparison on the line it was editing, so the file still carries the bug the PR was opened to close. Found in branch `test/gateway-alert-debug`, writing tests for `debug/`, which had none — **not** PR #104, which is where **A-013** came from. Left unfixed deliberately: a production-code fix inside a tests-only change is the drive-by root `AGENTS.md` rule 2 prohibits
- [ ] **A-012** `low` Register `DebugModule` conditionally instead of unconditionally — it sits in `AppModule.imports` at `src/app.module.ts:189` on every deploy, so `debugMyStorage` is introspectable and callable in production and only the service-level `NODE_ENV` check stops it from answering. **This is surface area, not a bypass:** the gate itself works and is now well tested — a mutant that moves it after both DB reads is killed by a dedicated `not.toHaveBeenCalled()` assertion. Conditional registration would remove the surface rather than guard it. Filed separately from **A-011** rather than merged into it, because A-011 also covers GraphiQL and has nothing to do with this module; but the two **compound** — A-011's exact-match comparison is the whole of what currently stands between this query and the internet, so A-011 is the one to do first and this one loses most of its urgency once it lands
- [ ] **A-013** `medium` Make `passkeyRegisterVerify` answer 400, not 500, on a malformed `credentialJson` — the client gets `INTERNAL_SERVER_ERROR` where `BAD_USER_INPUT` is correct, so a client-side bug reads to the mobile app as a server outage. Mechanism: `parseCredential` is evaluated lazily _inside_ the `callAuth` callback, so its `BadRequestException` is caught by `callAuth`'s try/catch; `readStatus` then looks for `statusCode` — the field Better Auth's `APIError` carries — while a NestJS `HttpException` exposes `status`, the lookup misses, and it falls through to 500, which `errorFormatter` maps to `extensions.code`. The DTO's `@MaxLength`/`@IsNotEmpty` cannot catch this: they never check that the string parses. **Scope precisely** — `AuthService.callAuth` is character-identical and carries the same latent trap, so fix both copies of the helper together, but there is **no reachable instance in `auth/` today**: all five call sites (lines 92, 147, 191, 369, 483) were read, none of their callbacks can throw an `HttpException`, and `headersForSession()` is awaited outside `callAuth` at line 367. **Whoever closes this must update, not delete, the characterization test at `security.service.spec.ts:585-618`** — it pins the current 500 on purpose and its docblock names the mechanism, the client impact, the owner, two candidate fixes, and this instruction. Deleting it is the natural move when the behaviour changes and would silently drop the assertion that a malformed blob never reaches Better Auth
- [ ] **A-014** `low` **Decision, not implementation:** should a `full`-permission caregiver be able to mark a patient's alerts read? Today they can _read_ via `alerts(patientId:)` but `markAlertRead` / `markAllAlertsRead` have no caregiver path at all. That asymmetry may well be right — the recipient arguably owns their own read state, and a caregiver clearing a patient's badge could hide an alert the patient never saw — but it is undocumented, so the next person to read the resolvers cannot tell an intentional boundary from an oversight. Pinned in the specs with a comment rather than asserted as correct, deliberately: asserting it would freeze a decision nobody has made. Like **A-010** and **I-004**, this needs a call written down, either direction; it is **not a ticket to pick up and implement**
- [ ] **A-015** `medium` Extract the three `better-auth.ts` boot guards into `auth/boot-guards.ts` so a spec can load them — **triggered, not scheduled: the next change that touches a guard in `better-auth.ts` extracts it first.** Same conditional shape as **I-004**, and the same reason: there is no deadline at which this becomes urgent, but there is a moment at which not having done it is negligent. **What forced the entry.** After A-011 routed all six sites through `isProduction()`, `nest-reviewer` ran a mutant nobody had thought to run: invert the polarity at all five untested `NODE_ENV` sites **simultaneously** — CORS enabled only in production, token-bearing 404s logged only in production, and all three `better-auth.ts` refuse-to-boot guards effectively deleted. **The suite stayed fully green, 29 suites / 521 tests.** So the test surface is blind to precisely the catastrophe A-011 exists to prevent: A-011 proves `isProduction()` answers correctly, and nothing proves any call site does the right thing with the answer. Closing A-011 does not close that, which is why this is a separate entry rather than a line in it. **The cause is structural, not neglect** — `better-auth.ts` imports ESM-only `better-auth` while the Jest setup is CJS, so no spec can load it; `resolveAuthBaseURL` is unexported; and `better-auth.controller.ts` is unloadable for the same reason, reaching the ESM graph through `better-auth.provider.ts:6`. Nobody skipped these tests; they were unwritable. **The controller half may be a one-line import fix, not an extraction — verify this first, because it changes the size of the task.** `auth/better-auth.token.ts` already exists to hold `BETTER_AUTH` and `InjectBetterAuth` away from the ESM graph and imports nothing but `@nestjs/common`, but `better-auth.controller.ts:5` imports both from `./better-auth.provider` instead, and `better-auth.provider.ts:6` value-imports `createBetterAuth` from `./better-auth`. Re-pointing that one import at `./better-auth.token` plausibly makes the controller loadable: its only other edge into that graph is line 6, `import type { BetterAuthInstance }`, which erases at compile. `nest-dev` found this while fixing its own comment and deliberately did not touch it — pre-existing, and unrelated to A-011. **Confirm by making a spec load the controller, not by re-reading the imports.** That claim was reasoned from the import chain rather than from a passing spec, and a value import anywhere else in the graph defeats it. If it holds, the real extraction work is only the three `better-auth.ts` guards. **Assume nothing about any file being ESM-clean without walking its whole chain** — that assumption has already been wrong twice in this one change: `env.ts`'s header originally cited `better-auth.controller.ts` as a file the import-nothing rule protects, which it does not, and the sentence had to be corrected. **The remedy has precedent twice in this service** — `auth/android-origin.ts` and `push/expo-push.client.ts` were both extracted for exactly this, and the new `src/env.ts` is a third instance whose docblock already explains the import-nothing discipline. `auth/boot-guards.ts` holds the three predicates, imports nothing, and becomes testable by existing under a different name. `medium` and not `high`: this is untested behaviour rather than broken behaviour, and the guards are correct today — but it escalates the moment someone edits one, which is what the trigger encodes
- [ ] **A-016** `medium` Fail fast on an unrecognised `NODE_ENV` at boot — `NODE_ENV=prod` is not production at any gate, was not before A-011, and normalisation does not close it: trimming and lowercasing maps `PROD` to `prod`, which still is not `production`. `nest-dev` pinned it as an explicit non-production assertion so that a future "match prefixes" change has to delete a rationale rather than quietly widen a security comparison, and `nest-reviewer` endorsed that — the pin is right and should stay. But `prod` is at least as plausible a deploy typo as `PRODUCTION`, and it opens all six gates in A-011 exactly as the original bug did. **The answer is not fuzzy matching.** Prefix or fuzzy comparison makes a security gate guess at operator intent, and the guess is unbounded — `production-eu`, `preproduction`, and `non-production` all match a naive prefix, and the last two mean the opposite of what they would be read as. Refusing to boot on a value that is not on a known list closes `prod`, `producton`, and `Prod` at once, and cannot be widened by accident. **Do this instead of extending A-011, not after it** — a boot-time allowlist makes A-011's normalisation partly redundant, so building both in sequence means writing a second mechanism and then discovering the first was a subset of it. **Two traps for whoever picks it up:** the allowlist must include every value actually in use (`development`, `test`, `staging`, `production` at least — `env.ts`'s docblock enumerates what the gates currently treat as non-production, and a list narrower than that refuses to boot a working deploy), and **unset must stay legal and non-production**, because a bare `pnpm start` sets no `NODE_ENV` and failing there would break local development to fix a production typo. This is a behaviour change — a process that used to start now exits — so it is explicitly **not** part of A-011 and wants its own review
- [ ] **A-010** `medium` **Decision, not implementation:** should a caregiver be able to edit their patient's name? (FR-01.3) — the requirement lists name, age, weight, height, congenital disease. `UpdatePatientHealthInput` (`schema.gql:609-615`) carries `dob`, `gender`, `weight`, `height`, `congenitalDisease`: age is covered by `dob`, name is not. There is no other route — `updateProfile` is the only mutation touching `firstname`/`lastname` and it is hard-scoped to `@CurrentUser()` with no `patientId` argument. This is **not an oversight**: `firstname`/`lastname` were deliberately excluded when the feature was built, on the reasoning that a name is how other people identify the patient rather than health data — the same judgement that excluded `email` and `phone`, which are unique Better Auth sign-in identities. So this entry is a product-owner call (follow the requirement, or keep the narrower scope and amend the requirement), **not a ticket to pick up and implement**. Do not add the fields until that call is made

### ai-service

- [x] **AI-001** `high` Replace stub OCR with full SYS / DIA / pulse extraction pipeline
- [x] **AI-002** `medium` Return confidence scores per field in the analysis reply payload
- [ ] **AI-003** `low` Add structured logging for every Redis message received and sent

### infra

- [ ] **I-001** `medium` Add a `staging` compose profile that mirrors prod minus S3
- [ ] **I-002** `low` Add healthcheck stanzas to all services in docker-compose.yml
- [ ] **I-003** `high` Land the build config push notifications need before a caregiver can receive anything — **what remains is five console/CLI steps only the account owner can do**, written up in full in `docs/guides/push-notifications-setup.md`: register the Android app in Firebase under `com.project.bpmonitor`, commit `google-services.json` at `client/google-services.json` (**not** `client/android/app/`, which `client/.gitignore:47` silently ignores), generate a service-account key, upload it as FCM V1 via `eas credentials`, and `eas env:create` `EXPO_PUBLIC_API_URL` in the **`production`** environment — `preview` already has a value (see **I-004**), `production` has none, and without it a production build installs fine and then throws on every GraphQL call, which `development` will never reveal because a dev client takes its host from Metro. The repo-side config is committed on branch `feat/i-003-push-build-config` and merges to `main` with this PR: `client/eas.json`, `android.googleServicesFile` in `app.json`, and `client/android/` untracked via `git rm -r --cached` — that last one is load-bearing, because `eas-cli@19.0.8` resolves a tracked `android/` to the `GENERIC` workflow and skips prebuild entirely, so all four Firebase steps could have been completed and push would still have delivered nothing from a green build. Do not re-commit the prebuild output. Filed separately rather than reopening A-009 so that a reader seeing A-009 closed does not conclude notifications are live — closed code plus missing config is a built feature, not a working one. `high` for exactly that reason
- [ ] **I-004** `medium` Point the `preview` EAS environment's `EXPO_PUBLIC_API_URL` at a stable host — it is currently a `trycloudflare.com` quick-tunnel URL, and those hostnames are allocated per tunnel process rather than reserved: the moment the tunnel restarts the name stops resolving, and every `preview` build ever produced against it is permanently broken. **It presents as a network error, not a configuration error** — the build is green, the app installs, and the failure surfaces days later as every GraphQL call timing out, which reads like the gateway being down or the tester's wifi. Nobody re-checks an env var that was set correctly and never edited. Needs either a reserved hostname (named Cloudflare tunnel, or any stable staging origin — see **I-001**) or an explicit decision that `preview` is throwaway and its URL gets re-set per session, which is a workable answer but has to be written down rather than assumed. **Not part of I-003**, which is push delivery only: this breaks a `preview` build's entire API surface whether or not push is configured. `medium` rather than `high` because no `preview` build is currently being relied on — it escalates the moment one is handed to a tester

---

## Blocked

<!-- Tasks that cannot proceed until an external dependency is resolved. -->
<!-- Format: - [!] **ID** `priority` description — blocked: <reason> -->

---

## Functional-requirements audit — 2026-08-06 (`baa013da`)

Everything below was verified against code, not documentation. A `TASK.md`
entry is not an implementation and neither is a doc claim.

**Score: 12 Must-have requirements — 9 DONE, 3 PARTIAL, 0 MISSING.** The one
Should-have (gallery import) is DONE. The three PARTIALs are tracked as
**A-009** (`high`), **C-007** (`medium`), and **A-010** (`medium`, a decision
rather than a ticket).

> **Moved since this snapshot (2026-08-07):** C-007 shipped (PR #96), so
> FR-03.2's PARTIAL no longer holds as of that date. The score above is left
> as it was measured at `baa013da` — a snapshot that gets silently updated
> stops being evidence of anything. A-010 remains an open product-owner
> decision, and A-009's code is closed but undeliverable until **I-003**'s
> build config lands.

### Gateway spec coverage — 2026-08-07

Not a work item; a state of the world that is easy to lose track of. **PR #104**
(`test/gateway-security-module`, covering `security/`) is open, and branch
`test/gateway-alert-debug` (the last two zero-spec modules) is about to open a
second. **When both merge, every gateway module has at least one spec** — the
first time that has been true.

The claim worth keeping is the coverage milestone, not a test count: a count is
a snapshot of a moving number, and this one already moved. `test/gateway-alert-debug`
was written up as +69, then review asked for a fix that replaced an arity
assertion with one reading the declared `@Args` set from `PARAM_ARGS_METADATA`,
and it shipped at **+72** (310 → 382, measured by `tester`). Both figures were
true when written. For the current number, run the suite rather than trusting
this file — `pnpm exec jest --watchman=false` from `server/app/api-gateway/`.

Four findings came out of writing those tests against code that had none. All
four are on the board above as **A-011**, **A-012**, **A-013**, **A-014**, and
**none is fixed**: each was left alone on purpose, because fixing production
code inside a tests-only change is the drive-by root `AGENTS.md` rule 2
prohibits. Two of them (**A-013**, **A-014**) are pinned by tests that assert
current behaviour rather than correct behaviour — read those entries before
touching the specs they name.

### Findings, not work items

These came out of the same audit. None is a task; each is a fact about the
system that a future reader should not have to rediscover.

- **FR-01.1 has no "username".** Identity is phone or email. More narrowly,
  the _app_ only offers phone — `LoginInput` is `phone: String!` with no email
  variant, and no client surface reaches Better Auth's configured
  `/sign-in/email` route. "Sign in with email" is true of the server and false
  of the app.
- **FR-02.3 has two extraction engines** — online via `ai-service` over Redis,
  offline via the native Kotlin `bp-vision` module. The requirement assumes one.
- **FR-03.1's timestamp is the device clock, uncorrected.** A patient with a
  wrong phone clock files readings at the wrong time and the trend chart orders
  them wrongly. Literally compliant; "accurate" is doing quiet work in that
  sentence.
- **FR-03.4's classification is computed client-side and stored.** A reading
  fetched back renders whatever status was written to it, including by an older
  build with different thresholds.
