# BP Monitor — Task Board

_Last updated: 2026-08-18 · Updated by bp-task_

## Imports

<!-- bp-task resolves these at read time. Tasks from imported files are merged
     into the board with their scope prefix. Duplicates are skipped. -->
@docs/project/api-gateway-plan.md

---

## Tasks

### client

- [ ] **C-001** `high` Wire caregiver push-notification preference screen to the store
- [ ] **C-002** `medium` Add 7-day BP trend chart to the history tab
- [ ] **C-003** `medium` Implement PIN / biometric lock for the app-open flow
- [x] **C-004** `high` Integrate on-device YOLO pre-flight result into camera UI warning banner — closed 2026-08-06 as superseded, not implemented: the live framing gate replaced the warning-banner design and already shipped (`docs/project/CLIENT-remaining.md` §10)
- [ ] **C-005** `medium` Restore the caregiver "ดูข้อมูล" jump from invitations into a patient's readings — ship with the home/history tab port; `client/src/app/invitations.tsx` should set the viewing context in `client/src/modules/caregivers/`, not the auth store (see `docs/project/CLIENT-auth-structure.md`, "activePatientId")
- [ ] **C-006** `medium` Stop `formatErrorMessage` leaking English into user-facing errors — `client/src/services/api.ts:107` prepends `"<OperationName> failed: "`, and `stripCode` in `client/src/lib/error-message.ts:23` is anchored (`/^\s*\[[A-Z_]+\]\s*/`) so it never strips the code; affects all six call sites (post, invitations ×3, post/[id] ×2, readings, community) — preferred fix is dropping the operation-name prefix, since it is developer data
- [x] **C-007** `medium` Add a severity filter to reading history (FR-03.2) — done 2026-08-07, branch `client/history-severity-filter`, commit `84f427ca`, open as **PR #96**. Four pills that **partition** `BPStatus` rather than overlap: ทุกระดับ · ปกติ (`normal`) · เฝ้าระวัง (`low`, `elevated`) · สูง/สูงมาก (`high`, `critical`). `low` sits with เฝ้าระวัง because a taxonomy organised around the word "high" silently orphans hypotension; the partition is asserted against the exported `BP_STATUSES`, so a sixth status fails a test rather than landing in no group. **The severity filter scopes the list, not the chart** — the chart is fed from the time-filtered array only, because a trend line through only the critical points hides every normal reading between them and a mostly-fine patient would read as being in continuous crisis. That asymmetry is deliberate; do not "fix" it into consistency. `history-list.tsx` got severity but not period, and the group carries over as a **parsed** route param falling back to "everything", so a hand-edited deep link cannot render an empty list — that screen had no test file at all before. **The daily time bucket was considered and rejected, not overlooked:** a fifth time pill while a second filter row competes for the same vertical budget is the wrong trade against a 44dp tap target for an elderly-first audience; the reasoning is written up in `docs/project/CLIENT-history.md`. 56 suites / 713 tests (from 54 / 659); GraphQL operations held at 50, which was a requirement — the filter is local, so any movement would have meant the screen started asking the server to filter
- [x] **C-008** `low` Delete the stale comment at `client/src/app/patient-health.tsx:39-41` — done 2026-08-07, same branch as C-007. It claimed `myPatients` returns neither `gender` nor `congenitalDisease` and that those two fields therefore start blank with a note explaining why; commit `e15b1118` had added both to `PatientSummaryType` and deleted that note. The comment is now rewritten rather than merely removed: it says all five fields seed, and explains that the submit is still a **patch** for a different reason — two caregivers can look after the same patient, so a full-form write would let the second to submit silently revert a field the first had just changed
- [ ] **C-009** `high` Fix the "เขียนโพสต์" compose button rendering top-left instead of as a bottom-right FAB on the community tab — **reported by a user; cosmetic, but on the main community tab and visible to everyone immediately.** There is exactly one compose entry point: `client/src/app/(tabs)/post.tsx:168-185`, `testID="community-compose"`. **The code already intends bottom-right, so this is a regression or a styling failure, not a design question:** it sets `className="absolute right-5 flex-row items-center rounded-full px-5"` together with `style={({ pressed }) => ({ bottom: insets.bottom + 80, minHeight: 56, backgroundColor: colors.primary, opacity: pressed ? 0.9 : 1 })}`, and the `FlatList` at `:146-150` sets `paddingBottom: insets.bottom + 140` with the comment "Clears the tab bar plus the floating compose button above it" — the layout below was designed around a floating button that is no longer floating. **Leading hypothesis, stated as a hypothesis because it was reasoned from the code and NOT reproduced on a device:** the `className` is dropped because `style` is the _function_ form (`({ pressed }) => ...`) that `Pressable` uses for press state; NativeWind is `^4.2.6` (`client/package.json:50`). If that combination discards the className styles, the element loses `position: absolute` and `right-5`, and the surviving inline `bottom` does nothing without a position, so the button falls back into normal flow. If the same mechanism also drops `className="flex-1"` from the `FlatList` at `:145`, the list sizes to its content instead of filling, putting the button directly beneath a short feed — which is exactly the reported "top left". **Counter-evidence that has to be reconciled, do not skip it:** `client/src/app/(tabs)/camera.tsx` uses `className="absolute ..."` in at least ten places (`:718`, `:814`, `:862`, `:923`, ...) and those work — but they are `View`s with a static `style` or none, not a `Pressable` with a function `style`. **That difference is the thing to test; if it does not hold, the hypothesis is wrong and the real cause is elsewhere.** **Test gap to close in the same change:** `client/__test__/screens/community.test.tsx:71` asserts only `expect(view.getByTestId('community-compose')).toBeOnTheScreen()` — presence, not position, which is precisely why a screen test stayed green while this reached a user. Whoever fixes it should assert the resolved style (position / right / bottom), or the same regression lands again silently. `medium` rather than `low` because if the hypothesis is right, the broken styling combination may be silently live at other call sites too.
  **Update 2026-08-18 — the hypothesis is now confirmed, not just reasoned, and it has already recurred four times.** Fixing `client/src/modules/caregivers/components/person-card.tsx`'s permission chip (closed separately as **C-021**) reproduced the exact mechanism this entry predicted: a NativeWind `className` mixed with a function-form `style` on a `Pressable` silently drops every class-derived key. `expo-reviewer` proved it by reverting the fix, running a new regression test, and confirming the resolved style kept only the inline object's `backgroundColor`/`borderColor`/`opacity` — then confirmed the mechanism by reading `react-native-css-interop`'s source directly: a function passed as `style` contributes no style keys to the merge. This is no longer a hypothesis.
  **Three more sites carrying the identical bug, found while chasing the one above:** (1) `client/src/app/reminders.tsx`'s `ReminderTimeRow` delete button — freshly introduced in this same session's reminders redesign (**C-024**), copied from a broken reference pattern; caught by `expo-reviewer` before merge and **fixed**, same before/after-revert proof. (2) `person-card.tsx`'s own `onOpen` wrapper (`:185-194`ish) and `onRemove` button (`:200-214`ish) — confirmed by `expo-reviewer`, **not fixed, out of scope**. (3) `client/src/modules/caregivers/components/link-row.tsx`'s remove button (`:125-139`ish) — confirmed by `expo-reviewer`, **not fixed, out of scope**; this is also the file `reminders.tsx`'s now-fixed button was originally copied from, which is how the bug propagated into new code.
  **Priority raised `medium` → `high`:** four occurrences (two fixed elsewhere, two still open) with a confirmed mechanism is a systemic pattern, not a single cosmetic regression — and the original site named in this entry, the community compose button, is still broken on the main tab.
  **Recommended next step, not yet scoped as its own ticket:** a dedicated sweep of every `Pressable` in `client/` for the `className` + function-form `style` combination, ideally done alongside fixing this entry's own compose button rather than as a separate pass.
- [x] **C-010** `high` Raise the client's phone ceiling to the gateway's, so a legal account is not unreachable from the app — closed 2026-08-17. `isValidPhone` at `client/src/modules/auth/lib/validation.ts:30` is `/^\d{9,10}$/`; the gateway's `PHONE_REGEX` is `/^[0-9]{9,15}$/` (`server/app/api-gateway/src/auth/types/auth.types.ts:3`, commented "covers TH + intl") and is what actually gates `RegisterInput.phone`, `LoginInput.phone`, and `UpdateProfileInput.phone`. **The file breaks a rule written in its own docblock at `:6-8`** — "nothing here may be stricter than `RegisterInput`'s class-validator decorators, or a legitimate account becomes unreachable from the app" — which is why this is `high` and not a cosmetic mismatch: an 11-15 digit number is legal server-side and cannot be registered with, signed in with, or saved from the profile screen. **There is no route around it**, because `client/src/modules/profile/lib/validation.ts:82` imports the same predicate rather than writing its own, so the ceiling is identical on both screens. That reuse is the right shape and should stay — the fix is the regex, not a second copy. **Three Thai strings move with it** and are easy to leave stale, since none of them lives next to the regex: `auth/lib/validation.ts:44` and `:64` and `profile/lib/validation.ts:82` all read "เบอร์โทรศัพท์ต้องเป็นตัวเลข 9-10 หลัก"
  **Closed 2026-08-17.** The three Thai strings this entry flagged (`auth/lib/validation.ts:44`, `:64`, `profile/lib/validation.ts:82`) now read 9-15 to match.
  **A second, separate bug was found and fixed in the same pass, not just the predicate:** `client/src/utils/phone-format.ts`'s formatter was silently corrupting any number outside the two Thai shapes — `+14155551234` rendered as `14-155-512`, a different, shorter, still-plausible number, via an unconditional `slice(0, 9)`. Fixed to cap at 15 (matching the gateway) and render non-Thai-shaped input as plain ungrouped digits rather than inventing a grouping for an unknown country; the full reasoning now lives as a docblock in `phone-format.ts` itself.
  **One stale consumer found after the initial close, fixed in the same effort:** `client/src/app/(auth)/onboarding-phone.tsx:41` still said "9-10 หลัก" — corrected to "9-15 หลัก". This screen is currently unreachable pending Google OAuth credentials per its own docblock, so this was inert but worth closing rather than left as a landmine for whenever it is reconnected.
  **Test gap closed in the same change:** `phone-format.ts` had two untested branches (progressive typing without a leading zero, at 1-2 and 3-5 digits) — added; the file is now at 100% coverage.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that.
- [x] **C-011** `high` Validate the register screen's optional health fields, which today it does not check at all — closed 2026-08-17. `RegisterField` (`client/src/modules/auth/lib/validation.ts:19`) omits `dob`, `gender`, `weight`, `height`, and `congenitalDisease`; `validateRegister` never inspects them, and the screen has no error slot to render a message into. `optionalNumber` (`client/src/app/(auth)/register.tsx:49-54`) accepts any positive finite number, so a weight of 9999 reaches the gateway, is refused by `@Max(500)`, and fails the **entire** registration with an English class-validator message the user cannot attribute to any field — that last part compounds with **C-006** and gets worse, not better, if C-006 lands first. **The more durable half is the disagreement between two screens about the same columns.** `client/src/modules/profile/lib/validation.ts` already owns `WEIGHT_RANGE_KG` 20-300 (`:24`), `HEIGHT_RANGE_CM` 50-250 (`:25`), `CONGENITAL_DISEASE_MAX` 500 (`:27`), `MAX_AGE_YEARS` 120 (`:29`), plus `validateMeasurement` (`:41`) and `validateDob` (`:62`), and register applies none of them: a 10 kg weight registers successfully and can then **never** be re-saved from the profile screen, because the user's own edit form refuses a value their sign-up accepted. The docblocks at `:37-39` and `:56-60` already name "a second implementation" as the thing being guarded against, and register is that second implementation by omission rather than by copy-paste — which is why grepping for duplicated constants does not find it. `client/src/modules/caregivers/lib/health-form.ts` is a third consumer of the same validators and must keep agreeing. **Trap for whoever picks this up if the current change does not land:** `profile/lib/validation.ts:19` imports `isValidPhone` from `auth/lib/validation.ts`, so having auth import the health validators back from profile closes a module cycle. The shared constants need a neutral home, not a back-reference
  **Closed 2026-08-17**, via a new `client/src/lib/health-validation.ts` that imports nothing, avoiding exactly the cycle trap named above. `auth/lib/validation.ts`, `profile/lib/validation.ts`, and `modules/caregivers/lib/health-form.ts` all now consume it.
  **Bounds were widened to match the gateway exactly** — `WEIGHT_RANGE_KG: { min: 1, max: 500 }`, `HEIGHT_RANGE_CM: { min: 30, max: 280 }` — not to profile's old, narrower values, per an explicit user decision mid-session ("ขยายให้เท่า server").
  **One accepted, named trade-off:** the wider weight ceiling narrows the window for catching a slipped-zero typo — a true 31-50kg mistyped as 310-500 no longer exceeds the new ceiling, where it exceeded the old 300kg one. Recorded in `health-validation.ts`'s own docblock rather than silently accepted.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that.
- [x] **C-012** `medium` Replace the register screen's hand-rolled date picker with the shared `DateField`, so an optional date of birth can be cleared — closed 2026-08-17. This is the user-reported symptom that started the whole cluster. `client/src/app/(auth)/register.tsx:196-217` duplicates `client/src/modules/profile/components/date-field.tsx`, and the register copy has no clear button, so an optional field becomes permanently set the moment it is touched. Filed separately from **C-011** on purpose although both screens are the same screen: C-011 is a missing validator call, this is a duplicated component, and the fix for one does not produce the fix for the other — deleting the duplicate is what closes this, and it also happens to close three divergences for free. **Three more, all inherited from the duplication rather than decided:** the copy skips `validateDob`, so an age over 120 years goes to the server unchecked where profile catches it locally; it renders `DateTimePicker` with no web guard even though the package has no react-native-web build, while `date-field.tsx:50` and `:98-102` degrade gracefully; and it seeds the spinner at `new Date(1970, 0, 1)` where `date-field.tsx:141-146` seeds at now − 60 years for a documented elderly-first reason, so the register screen makes the target user scroll further than the profile screen does. **One line so it is not rediscovered:** `date-field.tsx:117` uses the `onChange` prop, marked `@deprecated` at `@react-native-community/datetimepicker@9.1.0` `src/index.d.ts:43` in favour of the `onValueChange` + `onDismiss` pair the register screen already uses. Both work in 9.1.0, so this is an inconsistency to fold in while the file is open, not a break and not a reason to widen the change
  **Closed 2026-08-17.** `client/src/app/(auth)/register.tsx` now uses `date-field.tsx`'s `DateField`, which has a working clear control (`onChange(null)`) — verified by `expo-reviewer` by reading the wiring, not just trusting the report. This closes the original user complaint that started this entire cluster ("วันเกิด(ต้องแก้ได้อิสระ)"), and folds in the three inherited divergences named above for free, since the duplicate they lived in no longer exists.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that.
- [x] **C-013** `medium` Hide every passkey surface behind one shared kill-switch constant until the RP-ID configuration lands — closed 2026-08-17. This is a deliberate gate, not a defect, and specifically not dead code for a future cleanup to delete. Passkeys are a four-way configuration — `PASSKEY_RP_ID`, the signing-key fingerprint, a real HTTPS domain, and the package name must all agree, and three of four fails at the device with an error that reads like a server bug (root `AGENTS.md`, "Areas of special attention", and `server/app/api-gateway/AGENTS.md`). None of that is in place, so the surfaces are switched off behind one exported constant with **no code deleted on either side**. All four entry points are enumerated because a subset leaves a live path to a feature that cannot work, and the first two gate on _different_ conditions — which is the reason one shared flag was needed rather than a change to `isPasskeyAvailableOnDevice`:
  1. `client/src/app/(auth)/login.tsx:173-183` — the sign-in button, gated on `isPasskeyAvailableOnDevice()`, a **device** capability check. Its comment already explains that the button is omitted rather than disabled when unavailable, so the kill-switch extends an established pattern instead of introducing one.
  2. `client/src/app/security/index.tsx:79-92` — the security-hub row, gated on `overview?.passkeySupported`, a **server**-provided flag. Different condition, different source of truth: no single existing predicate covers both this and site 1, which is the whole argument for the shared constant.
  3. `client/src/modules/security/lib/security-posture.ts:65-74` — the `attention` posture that does not merely expose the feature but **actively recommends it** ("เพิ่ม Passkey ให้เข้าง่ายขึ้นได้") and routes to `/security/passkeys`. Missing this one is the worst version of a partial job: the app would nag the user toward a screen the other two gates just hid.
  4. `client/src/app/security/passkeys.tsx` — the management screen itself, registered at `client/src/app/security/_layout.tsx:14` and therefore reachable by deep link even with every row above hidden. A route stays addressable after its last visible link is removed.
  **Turning it back on is flipping the one constant, and it must not happen before the four-way configuration is verified end to end on a device** — a green build proves nothing here, because the failure is at registration time on the handset. **Scope is client-side visibility only:** the gateway resolvers and DTOs under `server/app/api-gateway/src/security/` are untouched and stay that way, so `passkeySupported` keeps answering honestly and no wire contract moves
  **Closed 2026-08-17.** `PASSKEY_ENABLED` in `client/src/modules/security/lib/feature-flags.ts` gates all four entry points enumerated above — the sign-in button, the security-hub row, the `attention` posture recommendation, and `/security/passkeys` itself, which stays registered at `security/_layout.tsx` and renders an unavailable state rather than being orphaned. Nothing was deleted on either side.
  **One gap accepted by explicit user decision, not by code:** a user whose _only_ sign-in method is a passkey loses their route back in once this flag is off, if they ever sign out. This was flagged to the user mid-session; they chose to ship the hide as-is rather than special-case it. `expo-reviewer` separately verified this is **not** the "you're fine" failure mode originally feared: `assessSecurity`'s untouched `risk` branch (`signInMethods <= 1 && !hasPassword`) still fires correctly and points the user to `/security/password`. Both facts hold at once — the accepted gap (no route back in for a passkey-only user who signs out) and the verified fact (they are told this, not silently stranded on a screen that claims they're fine).
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that.
- [x] **C-014** `medium` Hide the Google sign-in entry point behind its own kill-switch, alongside C-013's passkey hide but decided as a separate entry — closed 2026-08-17. Extends the same session's kill-switch pattern to a second, unrelated surface: `GOOGLE_SIGN_IN_ENABLED` in `client/src/modules/auth/lib/feature-flags.ts` (deliberately not the same file as `PASSKEY_ENABLED` — different owning domain) gates the single real entry point, `login.tsx`'s Google button, as `GOOGLE_SIGN_IN_ENABLED && isGoogleSignInConfigured()`.
  **Filed separately from C-013 rather than folded into it**, on the same reasoning C-011/C-012 were split: the mechanism looks identical (a boolean flag hiding a sign-in surface) but the _reason_ and the _reversal condition_ are not the same fact twice. Passkey is blocked on unlanded infrastructure — the RP-ID / signing-key / domain / package-name four-way config from C-013 — a technical gate that flips once that config is verified end to end on a device. Google's own docblock states its gate is a **product sign-off decision**, not a technical one: `isGoogleSignInConfigured()` already answers the technical question correctly and is untouched, so nothing here waits on infrastructure. A reader tracking "when can this come back" needs two different answers, which is what a shared entry would have hidden.
  **Only one entry point exists for Google**, unlike passkey's four: `security/index.tsx`'s Google row and the mentions in `profile.tsx` / `verify-email.tsx` are account-_linking_ status displays, not sign-in, and were correctly left untouched — hiding them would have hidden true information about an already-linked account, not a broken entry point.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that.
- [~] **C-015** `high` Fix the camera preview sometimes going black after using the gallery picker — **fix implemented, `expo-reviewer` APPROVED, and gate-passed; open only because it is not yet verified on a real device or emulator. Do not close this from a green build.** User-reported, intermittent ("บางครั้ง"). Root-caused, with appropriate hedging, to the **cancel** path specifically: `pickImage()` in `client/src/app/(tabs)/camera.tsx` used to return with no action when the gallery picker was cancelled, leaving the live camera preview in whatever state it was left in after being backgrounded by the system picker for as long as it was open. A _successful_ pick was never the problem — it immediately unmounts the live preview by setting `capturedImage`.
  **Fix:** `pickImage()` now calls the existing `retryCamera()` (reused, not reinvented) on cancel, forcing a remount. `expo-reviewer` verified the code itself is sound; the disclosed cost is real but bounded — every gallery cancel now pays a brief "กำลังเปิดกล้อง..." remount flash, not just the ones that were actually broken.
  **Why device verification was not attempted in this session, stated rather than silently skipped:** concurrent, unrelated auth-flow edits landing in the same tree at the time made a login-dependent manual pass unreliable evidence either way, and the bug is reported as intermittent, so one clean pass would not have been strong evidence regardless.
  **Repro steps for whoever verifies this:** (1) open the camera screen; (2) tap "แกลเลอรี่"; (3) tap Cancel/back on the system image picker — cancel it, do not pick an image; (4) confirm the live camera preview is usable immediately after; (5) repeat several times, since the original bug was intermittent, so a single clean pass does not confirm the fix and a single reproduction of the old bug does not rule it out either.
  **Context, not a separate action item:** the native Kotlin camera module (`client/modules/bp-vision/android/`) was deliberately left untouched — the fix is JS-level and platform-agnostic, covering both the native Android path and the `expo-camera` fallback, specifically because nobody could verify CameraX's actual failure mode without a device. If the JS-level remount turns out not to fix it, the native module is where to look next.
  Gate (code-level only, not device verification): `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that. None of this substitutes for the device pass above.
- [x] **C-016** `high` AI analysis result UX: auto-open the confirmation sheet on a finished online analysis, and tell the user when the AI genuinely could not read the display — closed 2026-08-17, two related fixes in one change.
  **Fix one — auto-open on completion.** `camera.tsx`'s online path filled the form on a confident result but never opened the entry sheet — the user had to notice the phase-pill change and manually tap "ยืนยันภาพ". Fixed in `use-camera-analysis.ts` via a synchronous `onSettled` callback, not a `.then()` continuation, specifically so React 18's automatic batching guarantees the phase-pill update and the sheet's visibility update land in the same commit — `expo-reviewer` verified this concurrency reasoning by reading the actual code shape, not on the author's word alone. **One deliberate, reviewed divergence:** the sheet does NOT auto-open on abort or hard analysis failure — the online path has its own existing failure-recovery banner on the photo-confirm screen, and popping the sheet on top would bury it. A manual "ยืนยันภาพและกรอกค่าความดัน" button remains available regardless, so this never blocks manual entry.
  **Fix two — surface a genuine read failure, not just low confidence.** User's explicit ask: "ถ้า ai อ่านค่าแล้ว ไม่ได้ ให้บอกผู้ใช้ด้วย". The gateway's wire contract already carried a real `status: 'unreadable'` value (confirmed by `expo-reviewer` against `server/app/api-gateway/src/ai/ai.service.ts:244-250` — it already flows from ai-service through the gateway to the client), but `use-camera-analysis.ts` never read `status` at all, only `confidence`. Now branches on it and shows a new banner (amber `statusColor.elevated`, matching the existing low-confidence banner's visual language, single "เข้าใจแล้ว กรอกด้วยตนเอง" dismiss since there's nothing to offer) — `expo-reviewer` independently agreed amber over red was the right call, reasoning this is a reading-quality outcome, not a broken-operation outcome, for a health-anxious elderly-first audience.
  **A parallel, previously-undocumented gap on the offline path was closed in the same change:** the on-device engine's "unavailable" result used to mean total silence _by design_, documented as deliberate in its own docblock, conflating "no engine exists on this platform" (structural, every attempt on iOS/web/Expo Go) with "the engine ran on this photo and genuinely failed" (rare, Android-only). A new `platformUnsupported` field now distinguishes them, and the docblock was rewritten to match — a deliberate override of a previously-documented decision, done on the user's explicit instruction, not a silent behavior change.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1798 tests, `test:screens` 76 suites/707 tests, full `pnpm test` 188 suites/2505 tests (three independent runs agreeing), `verify-models` OK on both models, `expo-doctor` 21/21. `expo-reviewer` returned **APPROVED** on the full diff before that.
- [ ] **C-017** `low` Have `closeCamera()` abort an in-flight online analysis the way `retake()` does — found and deliberately left unfixed during this session's C-016 work, recorded rather than silently dropped. `closeCamera()` in `client/src/app/(tabs)/camera.tsx` does not call `resetAnalysis()` the way `retake()` does, so closing the camera screen mid-analysis lets it keep running in the background. `expo-reviewer` judged this pre-existing and not worsened by this session's changes: the new `isMountedRef` guard (from C-016) makes the _sheet_ safe even if the analysis resolves late, but the wasted work itself is untouched either way.
- [ ] **C-018** `low` Consider remounting the camera preview between a successful gallery pick and the captured-image state being set — found and deliberately left unfixed during this session's C-015 work, recorded rather than silently dropped. Between a successful (non-cancelled) gallery pick and `capturedImage` being set, there is a brief window where a possibly-broken live preview is still visible on screen. Deliberately not remounted there: doing so would force a visible flicker on _every_ successful pick, judged a worse trade than the rare, bounded edge case it would guard against. `expo-reviewer` reviewed this reasoning and did not object.
- [x] **C-019** `low` Offer camera or gallery when editing the profile photo, instead of hardcoding gallery — closed 2026-08-18. `client/src/app/profile.tsx` previously hardcoded avatar changes to `changeAvatar('library')` even though `useProfileAvatar`'s `change(source: 'library' | 'camera')` (`client/src/modules/profile/hooks/use-profile-avatar.ts`) already fully implemented both.
  **Closed 2026-08-18.** Now offers an action-sheet choice, copying `avatar-picker.tsx`'s existing wording for consistency rather than inventing new copy.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1805 tests, `test:screens` 77 suites/730 tests, full `pnpm test` 189 suites/2535 tests (all reconciling exactly), `verify-models` OK on both models. `expo-doctor` was the only failure, 20/21 (16 Expo SDK packages patch-behind) — pre-existing repo debt, confirmed via zero uncommitted changes to `client/package.json`/`pnpm-lock.yaml` this session, tracked separately as **C-025** rather than blocking this batch. `expo-reviewer` returned **APPROVED** on the full `client/` batch, after one CHANGES_REQUIRED round (the health-edit-history background missing `ChangeLogEntryRow`, and the reminders.tsx delete button carrying C-009's Pressable/style bug) — both fixed and re-verified.
- [x] **C-020** `medium` Give caregiver health-edit-history real entry rows the same translucent background the placeholder states already had — closed 2026-08-18. First attempt landed on `profile-changes.tsx`'s placeholder `Card` only (error/loading/empty states), missing `ChangeLogEntryRow` (`client/src/modules/caregivers/components/change-log-entry.tsx`) — the component that renders every entry a patient with real history actually sees. Caught by `expo-reviewer`, confirmed with the user, corrected.
  **Closed 2026-08-18.** Both now use `rgba(255,255,255,0.85)` light / `rgba(255,255,255,0.12)` dark via `useColorSchemePreference()`, reasoned against `ThemedText`'s dark-mode near-white text color so the translucency doesn't wash out contrast.
  **Not verified on a real device or simulator** — reasoned through contrast math only; note this if the screen is ever revisited.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1805 tests, `test:screens` 77 suites/730 tests, full `pnpm test` 189 suites/2535 tests (all reconciling exactly), `verify-models` OK on both models. `expo-doctor` was the only failure, 20/21 (16 Expo SDK packages patch-behind) — pre-existing repo debt, confirmed via zero uncommitted changes to `client/package.json`/`pnpm-lock.yaml` this session, tracked separately as **C-025** rather than blocking this batch. `expo-reviewer` returned **APPROVED** on the full `client/` batch, after one CHANGES_REQUIRED round (this entry's placeholder-only miss, and the reminders.tsx delete button carrying C-009's Pressable/style bug) — both fixed and re-verified.
- [x] **C-021** `medium` Fix the caregiver permission chip losing its layout styles until tapped, in `person-card.tsx` — closed 2026-08-18, and now hard evidence for the open **C-009** hypothesis rather than a hunch. The chip mixed a NativeWind `className` with a function-form `style` on a `Pressable` — the exact combination C-009 hypothesizes silently drops the class-derived styles at `app/(tabs)/post.tsx`'s compose button.
  **Closed 2026-08-18.** Fixed by folding everything into one function-form `style` object.
  **Mechanism independently confirmed by `expo-reviewer`, not just asserted**: reverted the fix, ran a new regression test, and confirmed the resolved style genuinely lost `alignItems`, `borderRadius`, `borderWidth`, `flexDirection`, `paddingHorizontal`, `paddingVertical`, keeping only the inline object's `backgroundColor`/`borderColor`/`opacity` — then confirmed the mechanism by reading `react-native-css-interop`'s source directly: a function passed as `style` contributes no style keys to the merge.
  **Two more sites carrying the identical bug were found while fixing this one, neither fixed here — see the updated C-009 for the full account:** `person-card.tsx`'s own `onOpen` wrapper and `onRemove` button, and `client/src/modules/caregivers/components/link-row.tsx`'s remove button.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1805 tests, `test:screens` 77 suites/730 tests, full `pnpm test` 189 suites/2535 tests (all reconciling exactly), `verify-models` OK on both models. `expo-doctor` was the only failure, 20/21 (16 Expo SDK packages patch-behind) — pre-existing repo debt, confirmed via zero uncommitted changes to `client/package.json`/`pnpm-lock.yaml` this session, tracked separately as **C-025** rather than blocking this batch. `expo-reviewer` returned **APPROVED** on the full `client/` batch, after one CHANGES_REQUIRED round (findings unrelated to this entry — see C-020) — both fixed and re-verified.
- [x] **C-022** `low` Drop stale passkey copy from the security subtitle, and hide the Google account row too — closed 2026-08-18. `client/src/app/settings.tsx`'s "ความปลอดภัย" subtitle no longer mentions Passkey (dropped, not replaced with new copy).
  **Closed 2026-08-18.** `client/src/app/security/index.tsx`'s Google account row is now gated on `GOOGLE_SIGN_IN_ENABLED` — this **reverses** an earlier decision from this same session's first passkey/Google batch (**C-014**), where the row was deliberately left visible as "informational only"; the user asked for it hidden too, on direct instruction, not a bug fix.
  **Fixing this also required recomputing the `isLast` divider logic** across three now-conditionally-rendered rows (password/passkey/Google) so the group's bottom rounding never orphans against a hidden row — verified by `expo-reviewer` across all four flag combinations.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1805 tests, `test:screens` 77 suites/730 tests, full `pnpm test` 189 suites/2535 tests (all reconciling exactly), `verify-models` OK on both models. `expo-doctor` was the only failure, 20/21 (16 Expo SDK packages patch-behind) — pre-existing repo debt, confirmed via zero uncommitted changes to `client/package.json`/`pnpm-lock.yaml` this session, tracked separately as **C-025** rather than blocking this batch. `expo-reviewer` returned **APPROVED** on the full `client/` batch, after one CHANGES_REQUIRED round (findings unrelated to this entry — see C-020) — both fixed and re-verified.
- [x] **C-023** `medium` Add a 5-minute background grace period to app lock, and prefer fingerprint over face when both are enrolled — closed 2026-08-18. Previously re-locked immediately on every single background→active transition.
  **Closed 2026-08-18.** Now records a background timestamp and only re-locks if 5 minutes elapsed — a deliberate product decision (named constant, banking-app convention), purely additive to the existing single-navigator-wrap architecture, which already correctly locked only the first app entry and nothing else — no change needed there.
  **Separately:** `getBiometricCapability()` now checks fingerprint before face when both are enrolled (was the reverse) — a straight two-block reorder. Two existing tests updated with an explicit "reversed by decision" comment.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1805 tests, `test:screens` 77 suites/730 tests, full `pnpm test` 189 suites/2535 tests (all reconciling exactly), `verify-models` OK on both models. `expo-doctor` was the only failure, 20/21 (16 Expo SDK packages patch-behind) — pre-existing repo debt, confirmed via zero uncommitted changes to `client/package.json`/`pnpm-lock.yaml` this session, tracked separately as **C-025** rather than blocking this batch. `expo-reviewer` returned **APPROVED** on the full `client/` batch, after one CHANGES_REQUIRED round (findings unrelated to this entry — see C-020) — both fixed and re-verified.
- [x] **C-024** `high` Redesign reminder scheduling from interval+window to a free-form list of specific times, and fix a state-sync bug between two screens — closed 2026-08-18, a full redesign rather than a tweak.
  **State-sync bug, fixed with a real regression test.** `useReminderSettings()` held state in a per-instance `useState`, so `settings.tsx`'s subtitle and `reminders.tsx`'s switch could disagree after one screen wrote a change the other never saw. Migrated to a shared `@tanstack/react-query` cache (`setQueryData` after save, not `invalidateQueries` — reasoned as avoiding a pointless re-read of a value already known). `expo-reviewer` proved the fix is real by swapping the old hook back in and confirming the new "sharing settings across two consumers" tests fail against it.
  **Scheduling model changed, per explicit user request** ("like setting alarms," not "every N hours between start and end"). `ReminderSettings.reminderTimes: { hour, minute }[]` replaces `intervalHours`/`startHour`/`endHour`. The iOS 64-pending-notification budget protection (`SCHEDULED_NOTIFICATION_BUDGET = 56`) is preserved through two layers: the screen refuses to add a time or a day that would exceed the budget, with an explanation shown to the user (extended to guard day-selection too, not just time-adding, beyond what was literally asked); `schedule-plan.ts`'s capping logic still exists as a defensive fallback for old stored data, caps deterministically (earliest N times, applied identically across every selected day — verified to avoid the exact "Sunday full, Saturday none" trap the original code's docblock named), and surfaces a banner naming both the requested and actual counts. `expo-reviewer` verified the budget logic directly rather than trusting the description.
  **"ทดสอบการแจ้งเตือน" is now a popup (`Alert.alert`), not a banner at the top of the scroll** — fixes the reported complaint that users thought it silently failed because the confirmation appeared somewhere they weren't looking; the `notice` banner mechanism is preserved for its other, unrelated uses (permission-blocked/denied/unsupported outcomes).
  **The same Pressable-style bug confirmed on C-009/C-021 was found a third time in this same redesign, and fixed here:** the new `ReminderTimeRow` delete button copied a broken reference pattern from `link-row.tsx` (see C-009's updated note), mixing `className` with a function-form `style`. Freshly introduced in this same session, caught by `expo-reviewer` before merge and fixed the same way, verified with the same before/after-revert proof.
  **A pre-existing gap, explicitly not touched per instruction, now documented rather than hidden:** the `reconciledUserId` reconcile-guard (unrelated module-level state preventing a redundant OS-schedule re-registration) has an asymmetry where `update()` can trigger one redundant reconciliation call if a second screen mounts right after a live update — confirmed via `git diff` to predate this refactor, not introduced by it. A test now documents this as a known characteristic.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — `expo lint --max-warnings 0` clean, `tsc --noEmit` clean, `verify-graphql` 50/50 operations valid, `test:unit` 112 suites/1805 tests, `test:screens` 77 suites/730 tests, full `pnpm test` 189 suites/2535 tests (all reconciling exactly), `verify-models` OK on both models. `expo-doctor` was the only failure, 20/21 (16 Expo SDK packages patch-behind) — pre-existing repo debt, confirmed via zero uncommitted changes to `client/package.json`/`pnpm-lock.yaml` this session, tracked separately as **C-025** rather than blocking this batch. `expo-reviewer` returned **APPROVED** on the full `client/` batch, after one CHANGES_REQUIRED round (the health-edit-history background missing `ChangeLogEntryRow` — C-020 — and this entry's reminders.tsx delete button carrying C-009's Pressable/style bug) — both fixed and re-verified.
- [ ] **C-025** `medium` Bring 16 patch-behind Expo SDK packages current (`expo`, `expo-router`, `expo-notifications`, `expo-image-picker`, and 12 others) — `expo-doctor` currently reports 20/21; confirmed pre-existing, not caused by this session's work (zero uncommitted changes to `client/package.json` or `client/pnpm-lock.yaml`). The user explicitly chose to defer this rather than fold a 16-package dependency bump into the C-019–C-024 batch. Fix is `npx expo install --check` plus committing the resulting lockfile change. Needs its own dedicated test pass given the blast radius — 16 packages, including `expo-router` and `expo-notifications`, which this same session did substantial work on (C-024's reminders redesign uses `expo-notifications` directly).

### web

- [ ] **W-001** `high` Build clinician dashboard: reading list with BP-status filter
- [ ] **W-002** `medium` Add patient search and profile page
- [ ] **W-003** `low` Export readings as CSV from the web dashboard

### api-gateway

- [ ] **A-001** `high` Add pagination to `readings` GraphQL query
- [ ] **A-002** `medium` Rate-limit the `uploadBPImage` mutation per user
- [ ] **A-003** `low` Expose a `healthz` endpoint for the infra liveness probe
- [x] **A-004** `medium` Fix `addCaregiverPatient`'s unusable default relationship — already done in the code when this was audited on 2026-08-06: `VALID_RELATIONSHIPS` in `caregiver.service.ts` contains `caregiver`, with a comment recording exactly this bug (the schema default failed its own check and every invite relying on it was silently stored as `other`). The set was widened rather than the default changed, so existing clients keep working; `patient` stays out, because the column describes the caregiver's relationship _to_ the patient. Closed as already-implemented, not newly fixed
- [x] **A-005** `medium` Let a caregiver find a patient by email as well as phone in `addCaregiverPatient` — done 2026-08-06, both sides: the mutation argument is now the polymorphic `patientContact` (contains `@` ⇒ email, else phone; no `patientPhone` alias — the `CaregiverLinkType.patientPhone` _field_ is unchanged), and the client sends it from one untoggled input backed by `client/src/modules/caregivers/lib/contact.ts`
- [x] **A-006** `medium` Normalise `email` in `AuthService.updateProfile` — done 2026-08-06. `data.email` is now trimmed and lowercased **once, before the uniqueness check** (`server/app/api-gateway/src/auth/auth.service.ts:290`) and the normalised value is what gets patched (`:309`). Normalising at the top rather than only at the write fixes a second instance of the same bug that the original entry did not name: the pre-flight `findUnique({ where: { email } })` is exact-match on a `@unique` column, so a caller submitting `Foo@x.com` was checking a different key than the one being written — it could miss a real conflict and let the update fail at the database constraint instead of returning the intended `อีเมลนี้ถูกใช้งานแล้ว`. Still 0 bad rows, so no backfill was needed; this closes the only write path that could create one
- [x] **A-007** `high` Rate-limit `addCaregiverPatient` — done 2026-08-06: 10 attempts / 10 min, keyed on the **caregiver's user id** (keying on the contact string would let an attacker rotate addresses and never spend a budget) and counting every attempt, found or not. The honest `ไม่พบผู้ใช้จากอีเมลนี้` stays, as decided in A-005; this is its mitigation. The INCR + PEXPIRE Lua that used to live in `login-throttle.guard.ts` now lives in `server/app/api-gateway/src/redis/rate-limit.service.ts` — extracted out of `auth/better-auth.ts`, where Better Auth had inherited it, and covered by tests first. Refusal is a 429 carrying `retryAfterSec`, so the mobile countdown works unchanged. **Known and accepted:** the window is fixed, so 10 at 09:59 + 10 at 10:01 is 20 in two minutes; a sliding window would change login-throttle behaviour through the shared primitive — see A-008
- [x] **A-008** `low` Decide whether the shared rate limiter should slide instead of jump — **decided 2026-08-06: keep the fixed window, keep the budgets. Nothing was built; a trade-off was chosen.** The window starts on the first hit, not on a clock boundary, so a caller who spends the full budget at T and again at T+window gets 2x `max` back to back (Better Auth's credential routes 5/15min ⇒ a momentary 10; `addCaregiverPatient` 10/10min ⇒ a momentary 20). Accepted because the **sustained** rate is unchanged — only the momentary burst doubles — and both threats this limiter answers (credential stuffing, and the account enumeration A-007 mitigates) are priced in attempts per _hour_, not per second, so 2x for one instant barely moves an attacker's cost. Halving `max` was considered and **rejected, not overlooked**: three login attempts per fifteen minutes is not enough for a real user mistyping a password, so it degrades legitimate users to fix something that does not meaningfully help an attacker. A sliding window (sorted-set log, or two-bucket weighted counter) closes it precisely, but every consumer shares this one primitive — changing it changes Better Auth's credential routes too, and `consumeInMemory` would have to implement the same algorithm or a Redis outage would silently change semantics: large surface, small gain. **Revisit if:** a consumer needs a budget large enough that halving it is expensive; `retryAfter` needs to be second-accurate for UX; or a compliance rule names sliding windows. Full rationale lives as a doc comment above the `CONSUME` script in `server/app/api-gateway/src/redis/rate-limit.service.ts` — do not reopen this from first principles
- [x] **A-009** `high` Deliver push notifications for critical BP readings (FR-04.1) — **gateway half done 2026-08-07**; the client half is tracked separately as **C-001**. `PushToken` (`prisma/migrations/20260806164542_add_push_tokens`) is one row per app *installation*, keyed on a globally-`@unique` Expo token rather than on the user: the token belongs to the device, so a shared handset **reassigns** the row on the next `registerPushToken` instead of accumulating one per account — two rows would mean a patient's critical reading reaching whoever used the phone last. It deliberately does not hang off `user_sessions`, because a token must survive session rotation or a caregiver silently stops receiving alerts the moment their session refreshes; the price is that logout deletes it explicitly, which `logout(pushToken:)` / `logoutAllDevices(pushToken:)` now do (both arguments optional and additive — old call sites still compile). `ReadingService.createAlertForReading` sends via `PushService.notifyUsers` in `reading.service.ts`, fire-and-forget, reusing `getCaregiverAlertMessage` verbatim so there is one set of Thai copy rather than two. **Four gates stand in front of the send, and all four are decisions, not omissions.** (1) **Critical only** — `Alert` rows are still written for `warning`, but a caregiver pushed for every warning-level reading of a chronic-hypertension patient mutes the channel and then misses the one that mattered. (2) **Caregivers only** — the patient is holding the phone that just took the measurement. (3) **Staleness** — a reading whose `measuredAt` is more than 6 hours old does not push, because a notification saying "ค่าความดันวิกฤต" has to mean *now*: that is how the recipient reads it. A future `measuredAt` is a skewed device clock and counts as fresh. (4) **Burst** — at most one critical push per patient per 15 minutes across all their caregivers, through the existing `RateLimitService` rather than a second counter. Gates 3 and 4 were added by PR #137 because an offline sync turned into a burst of pushes: readings are captured offline-first and drained one mutation at a time, so a patient offline for three days arrived as several `createReading` calls, each firing its own push. **The order is load-bearing** — staleness is evaluated first so a backlog cannot spend the burst budget and silence a genuinely live reading that synced a second later, which is the one case the gates exist for. **Both fail open, and neither touches the alert rows**: rows are always written, only the interruption is withheld. **Known and accepted:** the staleness gate keys on `measuredAt`, which the client supplies, while the same PR stops trusting the client's `status` for exactly that kind of reason. The tension is deliberate — `measuredAt` has no better source in the current wire contract — and it costs this: a device whose clock runs more than six hours *behind* reality writes the alert rows and silently loses the caregiver interruption on a reading just classified `critical`. The recorded fix, not taken, is a `clientSentAt` field letting the gate use skew-corrected age; it adds a field to `CreateReadingInput` and crosses into `client/`. Dead tokens are pruned from both the send ticket and, 30 minutes later, the delivery receipt (`pending_receipt_id` + `@Cron` sweep) — receipt-stage `DeviceNotRegistered` is the common uninstall case, so ticket-only pruning would let the table grow forever. **Known and accepted:** the sweep's `@Cron` fires on every pod, which is safe here only because every step is idempotent (a receipt fetch is a read; the delete and the clear are already at their target state after the first run) — a *new* cron in this module has to make that argument or take a Redis lock. `EXPO_ACCESS_TOKEN` is optional and unset means Expo accepts a send from anyone holding one of our push tokens; set it in production. **Not exercisable in Expo Go** — Android dropped remote push in SDK 53, so the client half needs a dev build; the gateway treats "no token registered" as an ordinary outcome rather than an error precisely so that degrades honestly
<!-- FORMATTING TRAP — read before "tidying" any multi-line entry below.
     `web/src/lib/tasks.ts` parses this file. A task's note continues only
     while lines match /^\s+\S/ (:121) — indented, and CONTIGUOUS. A single
     blank line inside an entry hits `if (!raw.trim()) current = null` (:129)
     and every line after it is orphaned onto no task at all. A-011 spans the
     whole block below with no blank line in it, on purpose; adding one for
     readability silently deletes most of the board's most severe entry.
     Related: the priority must be exactly `high`, `medium`, or `low` (:52).
     A non-matching value — `critical`, say — does not merely fail to render:
     the line falls through to the continuation branch and is swallowed into
     the PREVIOUS task's note, corrupting a neighbouring entry too. -->
- [x] **A-011** `high` Compare `NODE_ENV` through one shared helper instead of an exact string match — **six non-spec sites, one change.** A deploy that sets `NODE_ENV=PRODUCTION` or `Production` — a plausible typo, not an exotic one — flips every one of them into its development branch at once. Enumerated so nobody has to re-grep, all verified by reading the body, not the grep line:
  1. `src/debug/debug.service.ts:27` (`=== 'production'`) — serves `debugMyStorage`, a per-user diff of Postgres rows cross-checked against S3 HEAD probes. See **A-012**.
  2. `src/app.module.ts:63` (`!== 'production'`) — **one gate deeper than an earlier draft of this entry claimed, and the correction matters:** this is inside `resolveGraphiqlEnabled`'s `value === ''` arm, reached only when `GRAPHIQL_ENABLED` is unset, empty, or whitespace. An explicit `1`/`true`/`yes`/`on` never consults `NODE_ENV` at all, and `app.module.spec.ts:404-410` already pins that. The exposure is still real — the committed `.env.example:12` ships `GRAPHIQL_ENABLED=` empty, so the unset arm **is** the default path — and it still ends in mutation-capable GraphiQL against live patient data, but it takes a second condition to get there. An over-scoped claim inside the entry that exists _because_ a finding was under-scoped would discredit the entry's own argument, so it is stated at its true width.
  3. `src/app.module.ts:135` (`!== 'production'`) — puts `validationErrors` into `extensions`, echoing raw submitted input back to the client.
  4. `src/main.ts:29` (`!== 'production'`) — calls `app.enableCors()` with **no allowlist**, so any origin may call the gateway. Auth is a bearer token rather than a cookie, so this is not CSRF; it is any page in any tab being able to use a token it has obtained.
  5. `src/auth/better-auth.ts:41` — `isDevelopment()`, used at **three** call sites, `:86`, `:537`, `:555`, which are **two kinds** of guard (the count is a deliberate grouping, not a miscount — say "three sites, two kinds" and both readings are true). All three **invert**: a missing `BETTER_AUTH_URL` stops refusing to boot (`:86`), and a missing delivery provider stops throwing for email (`:537`) and for SMS (`:555`). Worse than "mail goes to a stub" — `:537` then takes its `logger.debug` branch, and its own comment says the body carries a reset link or verification code, "a live credential — so it must never reach a production log". So this site does not merely fail to deliver auth mail; it writes credentials into production logs.
  6. `src/auth/better-auth.controller.ts:34` (`!== 'production'`) — debug-logs 404 paths that the comment beside it says carry tokens.
  **Severity is critical-grade; the label reads `high` for a mechanical reason, not a judgement.** On the evidence this was re-decided upward from `high`: the original `high` rested on the exposure being conditional on a misconfiguration nobody has observed, an argument that quietly assumed we would find out. Site 5 is what breaks it — the guards that exist precisely to refuse a misconfigured boot are themselves among what inverts, so such a deploy comes up green, sends no verification or reset mail, and logs live credentials, with nothing surfacing the cause. A conditional exposure that also disables its own detection is not the same risk as one that announces itself, and the aggregate is no longer one debug query plus a schema explorer. The label stays `high` because `web/src/lib/tasks.ts:52` matches `` `(high|medium|low)` `` only: writing `` `critical` `` silently drops the whole entry from the rendered board — the same failure that once lost the entire `ai-service` scope. **Treat this as the top item in `api-gateway` regardless of the backtick.** The contradiction has two halves in two files, and a follow-up needs both: `.claude/agents/bp-task.md:87` (`**Priority:** critical · high · medium · low`) and again at `:109` permit `critical`, while `web/src/lib/tasks.ts:52` rejects it. Not `TASK.md` itself — its only format comment is at `:75` and enumerates no values. Reconciling them (widen the parser, or drop `critical` from the agent definition) is a separate change, in files outside this one, and is deliberately not made here.
  **Fix all six behind one helper.** A fix scoped to the first two — the shape this entry originally described — closes two of six and leaves unrestricted CORS and the inverted fail-fast guards live behind a closed ticket. (An earlier draft justified the shared helper with "fixing either alone leaves the identical shape in the file next door"; that sentence was wrong on its own facts, since site 3 is 72 lines below site 2 in the _same_ file. The argument holds a fortiori and is restated above.) PR #102 is a worked example of the failure mode: it widened `GRAPHIQL_ENABLED`'s **value** parsing to a closed allowlist (`1`/`true`/`yes`/`on`) and never touched the `NODE_ENV` comparison on the line it was editing, so the file still carries the bug the PR was opened to close. Found in branch `test/gateway-alert-debug`, writing tests for `debug/`, which had none — **not** PR #104, which is where **A-013** came from. Left unfixed deliberately: a production-code fix inside a tests-only change is the drive-by root `AGENTS.md` rule 2 prohibits — **done 2026-08-07**, commit `626965bf`, merged as **PR #106** (`fix/node-env-production-helper`, merge `376a115d`). Verified on `origin/main`, not from the PR description: `src/env.ts:44` is the only `process.env.NODE_ENV` read left in non-spec gateway source, and all six sites above now call `isProduction()` — which greps as **eight** call sites, because sites 2 and 3 share `app.module.ts` and site 5's single `isDevelopment()` fanned out to three guards. `env.ts` imports nothing, and that is a constraint rather than a coincidence: it is pulled in by `auth/better-auth.ts`, whose ESM-only graph would break suite **parsing** — not one test — if anything propagated through it, so adding an import there is a wider change than it looks. `isDevelopment()` was **deleted rather than wrapped**, so the three refuse-to-boot guards now read `if (isProduction()) throw` instead of the double negative that obscured a refuse-to-boot rule as a development convenience. Suite 28/491 → 29/521, and `docs/reference/environment-variables.md` was corrected in the same change under rule 6. **Two findings survive this closure and are already carried on the board — do not re-file them.** First: only three of the six sites are exercised through production code, and `nest-reviewer` inverting the polarity at all five untested gates _simultaneously_ left the suite fully green — **A-015** is the remedy and already carries that green-suite number verbatim as the evidence justifying it, so the number does not need a second home here. Second: `NODE_ENV=prod` is still not production and stays pinned as an explicit non-production assertion — **A-016** closes it, and already says it must be built **instead of** extending this entry's normalisation rather than after it, which is why it was filed separately
- [ ] **A-012** `low` Register `DebugModule` conditionally instead of unconditionally — it sits in `AppModule.imports` at `src/app.module.ts:189` on every deploy, so `debugMyStorage` is introspectable and callable in production and only the service-level `NODE_ENV` check stops it from answering. **This is surface area, not a bypass:** the gate itself works and is now well tested — a mutant that moves it after both DB reads is killed by a dedicated `not.toHaveBeenCalled()` assertion. Conditional registration would remove the surface rather than guard it. Filed separately from **A-011** rather than merged into it, because A-011 also covers GraphiQL and has nothing to do with this module; but the two **compound** — A-011's exact-match comparison is the whole of what currently stands between this query and the internet, so A-011 is the one to do first and this one loses most of its urgency once it lands
- [ ] **A-013** `medium` Make `passkeyRegisterVerify` answer 400, not 500, on a malformed `credentialJson` — the client gets `INTERNAL_SERVER_ERROR` where `BAD_USER_INPUT` is correct, so a client-side bug reads to the mobile app as a server outage. Mechanism: `parseCredential` is evaluated lazily _inside_ the `callAuth` callback, so its `BadRequestException` is caught by `callAuth`'s try/catch; `readStatus` then looks for `statusCode` — the field Better Auth's `APIError` carries — while a NestJS `HttpException` exposes `status`, the lookup misses, and it falls through to 500, which `errorFormatter` maps to `extensions.code`. The DTO's `@MaxLength`/`@IsNotEmpty` cannot catch this: they never check that the string parses. **Scope precisely** — `AuthService.callAuth` is character-identical and carries the same latent trap, so fix both copies of the helper together, but there is **no reachable instance in `auth/` today**: all five call sites (lines 92, 147, 191, 369, 483) were read, none of their callbacks can throw an `HttpException`, and `headersForSession()` is awaited outside `callAuth` at line 367. **Whoever closes this must update, not delete, the characterization test at `security.service.spec.ts:585-618`** — it pins the current 500 on purpose and its docblock names the mechanism, the client impact, the owner, two candidate fixes, and this instruction. Deleting it is the natural move when the behaviour changes and would silently drop the assertion that a malformed blob never reaches Better Auth
- [ ] **A-014** `low` **Decision, not implementation:** should a `full`-permission caregiver be able to mark a patient's alerts read? Today they can _read_ via `alerts(patientId:)` but `markAlertRead` / `markAllAlertsRead` have no caregiver path at all. That asymmetry may well be right — the recipient arguably owns their own read state, and a caregiver clearing a patient's badge could hide an alert the patient never saw — but it is undocumented, so the next person to read the resolvers cannot tell an intentional boundary from an oversight. Pinned in the specs with a comment rather than asserted as correct, deliberately: asserting it would freeze a decision nobody has made. Like **A-010** and **I-004**, this needs a call written down, either direction; it is **not a ticket to pick up and implement**
- [ ] **A-015** `medium` Extract the three `better-auth.ts` boot guards into `auth/boot-guards.ts` so a spec can load them — **triggered, not scheduled: the next change that touches a guard in `better-auth.ts` extracts it first.** Same conditional shape as **I-004**, and the same reason: there is no deadline at which this becomes urgent, but there is a moment at which not having done it is negligent. **What forced the entry.** After A-011 routed all six sites through `isProduction()`, `nest-reviewer` ran a mutant nobody had thought to run: invert the polarity at all five untested `NODE_ENV` sites **simultaneously** — CORS enabled only in production, token-bearing 404s logged only in production, and all three `better-auth.ts` refuse-to-boot guards effectively deleted. **The suite stayed fully green, 29 suites / 521 tests.** So the test surface is blind to precisely the catastrophe A-011 exists to prevent: A-011 proves `isProduction()` answers correctly, and nothing proves any call site does the right thing with the answer. Closing A-011 does not close that, which is why this is a separate entry rather than a line in it. **The cause is structural, not neglect** — `better-auth.ts` imports ESM-only `better-auth` while the Jest setup is CJS, so no spec can load it; `resolveAuthBaseURL` is unexported; and `better-auth.controller.ts` is unloadable for the same reason, reaching the ESM graph through `better-auth.provider.ts:6`. Nobody skipped these tests; they were unwritable. **The controller half may be a one-line import fix, not an extraction — verify this first, because it changes the size of the task.** `auth/better-auth.token.ts` already exists to hold `BETTER_AUTH` and `InjectBetterAuth` away from the ESM graph and imports nothing but `@nestjs/common`, but `better-auth.controller.ts:5` imports both from `./better-auth.provider` instead, and `better-auth.provider.ts:6` value-imports `createBetterAuth` from `./better-auth`. Re-pointing that one import at `./better-auth.token` plausibly makes the controller loadable: its only other edge into that graph is line 6, `import type { BetterAuthInstance }`, which erases at compile. `nest-dev` found this while fixing its own comment and deliberately did not touch it — pre-existing, and unrelated to A-011. **Confirm by making a spec load the controller, not by re-reading the imports.** That claim was reasoned from the import chain rather than from a passing spec, and a value import anywhere else in the graph defeats it. If it holds, the real extraction work is only the three `better-auth.ts` guards. **Assume nothing about any file being ESM-clean without walking its whole chain** — that assumption has already been wrong twice in this one change: `env.ts`'s header originally cited `better-auth.controller.ts` as a file the import-nothing rule protects, which it does not, and the sentence had to be corrected. **The remedy has precedent twice in this service** — `auth/android-origin.ts` and `push/expo-push.client.ts` were both extracted for exactly this, and the new `src/env.ts` is a third instance whose docblock already explains the import-nothing discipline. `auth/boot-guards.ts` holds the three predicates, imports nothing, and becomes testable by existing under a different name. `medium` and not `high`: this is untested behaviour rather than broken behaviour, and the guards are correct today — but it escalates the moment someone edits one, which is what the trigger encodes
- [ ] **A-016** `medium` Fail fast on an unrecognised `NODE_ENV` at boot — `NODE_ENV=prod` is not production at any gate, was not before A-011, and normalisation does not close it: trimming and lowercasing maps `PROD` to `prod`, which still is not `production`. `nest-dev` pinned it as an explicit non-production assertion so that a future "match prefixes" change has to delete a rationale rather than quietly widen a security comparison, and `nest-reviewer` endorsed that — the pin is right and should stay. But `prod` is at least as plausible a deploy typo as `PRODUCTION`, and it opens all six gates in A-011 exactly as the original bug did. **The answer is not fuzzy matching.** Prefix or fuzzy comparison makes a security gate guess at operator intent, and the guess is unbounded — `production-eu`, `preproduction`, and `non-production` all match a naive prefix, and the last two mean the opposite of what they would be read as. Refusing to boot on a value that is not on a known list closes `prod`, `producton`, and `Prod` at once, and cannot be widened by accident. **Do this instead of extending A-011, not after it** — a boot-time allowlist makes A-011's normalisation partly redundant, so building both in sequence means writing a second mechanism and then discovering the first was a subset of it. **Two traps for whoever picks it up:** the allowlist must include every value actually in use (`development`, `test`, `staging`, `production` at least — `env.ts`'s docblock enumerates what the gates currently treat as non-production, and a list narrower than that refuses to boot a working deploy), and **unset must stay legal and non-production**, because a bare `pnpm start` sets no `NODE_ENV` and failing there would break local development to fix a production typo. This is a behaviour change — a process that used to start now exits — so it is explicitly **not** part of A-011 and wants its own review
- [ ] **A-010** `medium` **Decision, not implementation:** should a caregiver be able to edit their patient's name? (FR-01.3) — the requirement lists name, age, weight, height, congenital disease. `UpdatePatientHealthInput` (`schema.gql:609-615`) carries `dob`, `gender`, `weight`, `height`, `congenitalDisease`: age is covered by `dob`, name is not. There is no other route — `updateProfile` is the only mutation touching `firstname`/`lastname` and it is hard-scoped to `@CurrentUser()` with no `patientId` argument. This is **not an oversight**: `firstname`/`lastname` were deliberately excluded when the feature was built, on the reasoning that a name is how other people identify the patient rather than health data — the same judgement that excluded `email` and `phone`, which are unique Better Auth sign-in identities. So this entry is a product-owner call (follow the requirement, or keep the narrower scope and amend the requirement), **not a ticket to pick up and implement**. Do not add the fields until that call is made
- [x] **A-017** `medium` Stop showing naturally-expired sessions as active in device counts and the security-hub warning — closed 2026-08-18, root-caused entirely server-side. Same user-reported bug as the client's devices-screen complaint: `isActive` on `UserSession` is only ever flipped to `false` by explicit logout — nothing marks a session inactive on natural `expiresAt` expiry, so an abandoned or naturally-expired session stayed counted as active forever, and every fresh sign-in on the same device added another one.
  **Two related fixes, both read-side-only — no data mutation, no write-back.** (1) `AuthService.listSessions` — the value returned for each session's `isActive` is now computed as `session.isActive && session.expiresAt.getTime() > now`, rather than the raw column. Deliberately a computed field, not a `where`-clause filter, because the client's devices screen buckets sessions into "active" vs. "revoked/history" purely off this boolean — a filter would have made expired sessions vanish from history entirely instead of correctly falling into the revoked bucket. (2) `SecurityService.overview`'s `activeSessionCount` (feeds the security hub's "มี X อุปกรณ์ที่เข้าสู่ระบบอยู่" warning, found live by `nest-reviewer` during its first review as the same bug in a sibling path) — this one **is** a `where`-clause fix (`expiresAt: { gt: new Date() }`), correctly simpler since a plain count has no history-bucket requirement.
  **Verified, not just assumed, that this was never a live authorization hole:** `GqlAuthGuard` relies on Better Auth's own `getSession()`, which independently checks `expiresAt` and rejects an expired session before the gateway's own `isActive` check is ever reached — confirmed by reading the installed Better Auth source directly. This bug was display-only.
  **Explicit decision recorded, not defaulted into:** no lazy write-back was added to correct stale rows at the source (the `take: 20` window means it would still leave older rows stale forever, and this service's own working rules say not to add per-request writes). A dedicated `@Cron` sweep — mirroring `PushService`'s existing receipt sweep and `StorageCleanupService`'s existing orphan-image sweep — was proposed as real follow-up, not built here; tracked separately as **A-018**.
  **Client-side deliberately untouched:** `client/src/app/security/devices.tsx` was not the bug — this same QA pass's "device count keeps growing" complaint was root-caused entirely to this server-side gap.
  Gate: `tester` ran the full canonical suite independently and reported **PASSED** — typecheck clean, `pnpm exec jest --watchman=false` 33 suites/595 tests, zero skips. `nest-reviewer` returned **APPROVED** on the full gateway batch, after one follow-up round (findings raised, fixed, and re-verified, same pattern as the client batch above).
- [ ] **A-018** `low` Add a `@Cron` sweep to flip naturally-expired `UserSession` rows to `isActive: false` at the source — proposed as real follow-up during **A-017**, not built there: `AuthService.listSessions` and `SecurityService.overview` now compute/filter around expiry correctly on read, so this is data hygiene rather than a live bug. Mirror `PushService`'s existing receipt sweep and `StorageCleanupService`'s existing orphan-image sweep for the pattern. **Note carried over from A-017, not urgent enough for its own ticket:** `take: 20` + `orderBy: createdAt desc` on `listSessions` doesn't protect a legitimately-active session from falling out of the window if 20+ newer-but-abandoned rows accumulate ahead of it, because Better Auth's rolling session refresh touches `expiresAt`/`updatedAt` but never `createdAt` — narrow in practice, and this sweep would resolve it as a side effect.

### ai-service

- [x] **AI-001** `high` Replace stub OCR with full SYS / DIA / pulse extraction pipeline
- [x] **AI-002** `medium` Return confidence scores per field in the analysis reply payload
- [ ] **AI-003** `low` Add structured logging for every Redis message received and sent

### infra

- [ ] **I-001** `medium` Add a `staging` compose profile that mirrors prod minus S3
- [ ] **I-002** `low` Add healthcheck stanzas to all services in docker-compose.yml
- [ ] **I-003** `high` Land the FCM V1 credential push notifications need before a caregiver can receive anything — **three of the original five owner-only steps are now verified done against the live EAS account** (`bp-monitor-education-project`, projectId `b5572b71-c303-4b8e-b89e-14007c46ca3c`, checked 2026-08-10 as Owner); the setup write-up is `docs/guides/push-notifications-setup.md`. **Done: `EXPO_PUBLIC_API_URL` exists in the `production` environment**, `eas env:list --environment production` returns exactly `EXPO_PUBLIC_API_URL=https://api.bpmonapp.com/graphql` and nothing else. The earlier claim here — that `production` had no value and a production build would install fine and then throw on every GraphQL call — **no longer holds**. Record the value rather than just the fact, because it is the client half of a pair: it has to stay in step with `DOMAIN_NAME` / `BETTER_AUTH_URL` on the server, and nothing enforces that. **Done: `client/google-services.json` is committed and tracked** — `git ls-files client/google-services.json` returns it, at the correct path and not `client/android/app/`, which `client/.gitignore:47` would have silently ignored. **Done: Android build credentials exist and production builds work** — `eas build:list --platform android` shows a `finished` production build at version code 2 and a later one at version code 4 (canceled), so the keystore is on EAS and `autoIncrement` + `appVersionSource: "remote"` are functioning. **Still open, and unverifiable from the CLI:** register the Android app in Firebase under `com.project.bpmobile`, generate a service-account key, and upload it as **FCM V1** via `eas credentials`. These are _not_ confirmed either way — eas-cli exposes no non-interactive way to read Android credentials, so absence of evidence here is exactly that. **The trap to hold on to: `eas credentials -p android` holds three unrelated credentials, and having one implies nothing about the others** — (1) the **keystore**, used for _building_, confirmed present; (2) a **Google Service Account Key for FCM V1**, used for _push_, which is what this entry is about and is unconfirmed; (3) a **Google Service Account Key for Play submissions**, used for _submitting_, a different key again. Reading "credentials are set up" and concluding push works is wrong in both directions, which is why the three are named separately here. **Cross-reference, not scope:** PR #126 adds `.github/workflows/release-mobile.yml`, which builds and submits on push to `main`, and it is blocked on credential (3) plus one AAB having been uploaded to Play Console **by hand** — Google rejects an app's first upload over the API and the failure reads as `App not found`, which looks like a config error and is not one. Neither belongs to I-003's push work, but both live in the same `eas credentials` menu and the same Play Console, so whoever picks this up will be standing in front of them. **Still-true warnings from the repo-side config**, which landed on `main` (branch `feat/i-003-push-build-config`, **PR #103**, merged 2026-08-07 as `5212e129`: `client/eas.json`, `android.googleServicesFile` in `app.json`, and `client/android/` untracked via `git rm -r --cached`): `client/android/` must **stay** untracked, because `eas-cli@19.0.8` resolves a tracked `android/` to the `GENERIC` workflow and skips prebuild entirely — every Firebase step above could be complete and push would still deliver nothing from a green build. Do not re-commit the prebuild output. Stays open and stays `high`: push delivers nothing until the FCM V1 key lands, and A-009's gateway code is closed, so a reader seeing A-009 done would otherwise conclude notifications are live — closed code plus a missing credential is a built feature, not a working one
- [ ] **I-004** `medium` Point the `preview` EAS environment's `EXPO_PUBLIC_API_URL` at a stable host — it is currently a `trycloudflare.com` quick-tunnel URL, and those hostnames are allocated per tunnel process rather than reserved: the moment the tunnel restarts the name stops resolving, and every `preview` build ever produced against it is permanently broken. **It presents as a network error, not a configuration error** — the build is green, the app installs, and the failure surfaces days later as every GraphQL call timing out, which reads like the gateway being down or the tester's wifi. Nobody re-checks an env var that was set correctly and never edited. Needs either a reserved hostname (named Cloudflare tunnel, or any stable staging origin — see **I-001**) or an explicit decision that `preview` is throwaway and its URL gets re-set per session, which is a workable answer but has to be written down rather than assumed. **Not part of I-003**, which is push delivery only: this breaks a `preview` build's entire API surface whether or not push is configured. `medium` rather than `high` because no `preview` build is currently being relied on — it escalates the moment one is handed to a tester

---

## Blocked

<!-- Tasks that cannot proceed until an external dependency is resolved. -->
<!-- Format: - [!] **ID** `priority` description — blocked: <reason> -->

---

## Functional-requirements audit — 2026-08-06 (`baa013da`)

Everything below was verified against code, not documentation. A `TASK.md`
entry is not an implementation and neither is a doc claim.

**Score: 12 Must-have requirements — 9 DONE, 3 PARTIAL, 0 MISSING.** The one
Should-have (gallery import) is DONE. The three PARTIALs are tracked as
**A-009** (`high`), **C-007** (`medium`), and **A-010** (`medium`, a decision
rather than a ticket).

> **Moved since this snapshot (2026-08-07):** C-007 shipped (PR #96), so
> FR-03.2's PARTIAL no longer holds as of that date. The score above is left
> as it was measured at `baa013da` — a snapshot that gets silently updated
> stops being evidence of anything. A-010 remains an open product-owner
> decision, and A-009's code is closed but undeliverable until **I-003**'s
> build config lands.

### Gateway spec coverage — 2026-08-07

Not a work item; a state of the world that is easy to lose track of. **PR #104**
(`test/gateway-security-module`, covering `security/`) is open, and branch
`test/gateway-alert-debug` (the last two zero-spec modules) is about to open a
second. **When both merge, every gateway module has at least one spec** — the
first time that has been true.

The claim worth keeping is the coverage milestone, not a test count: a count is
a snapshot of a moving number, and this one already moved. `test/gateway-alert-debug`
was written up as +69, then review asked for a fix that replaced an arity
assertion with one reading the declared `@Args` set from `PARAM_ARGS_METADATA`,
and it shipped at **+72** (310 → 382, measured by `tester`). Both figures were
true when written. For the current number, run the suite rather than trusting
this file — `pnpm exec jest --watchman=false` from `server/app/api-gateway/`.

Four findings came out of writing those tests against code that had none. All
four are on the board above as **A-011**, **A-012**, **A-013**, **A-014**, and
**none is fixed**: each was left alone on purpose, because fixing production
code inside a tests-only change is the drive-by root `AGENTS.md` rule 2
prohibits. Two of them (**A-013**, **A-014**) are pinned by tests that assert
current behaviour rather than correct behaviour — read those entries before
touching the specs they name.

### Findings, not work items

These came out of the same audit. None is a task; each is a fact about the
system that a future reader should not have to rediscover.

- **FR-01.1 has no "username".** Identity is phone or email. More narrowly,
  the _app_ only offers phone — `LoginInput` is `phone: String!` with no email
  variant, and no client surface reaches Better Auth's configured
  `/sign-in/email` route. "Sign in with email" is true of the server and false
  of the app.
- **FR-02.3 has two extraction engines** — online via `ai-service` over Redis,
  offline via the native Kotlin `bp-vision` module. The requirement assumes one.
- **FR-03.1's timestamp is the device clock, uncorrected.** A patient with a
  wrong phone clock files readings at the wrong time and the trend chart orders
  them wrongly. Literally compliant; "accurate" is doing quiet work in that
  sentence.
- **FR-03.4's classification is computed client-side and stored.** A reading
  fetched back renders whatever status was written to it, including by an older
  build with different thresholds.
