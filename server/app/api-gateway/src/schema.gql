# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type BpAnalysisResult {
  """id ของผลการวิเคราะห์"""
  id: String!

  """ค่าความดันตัวบน"""
  systolic: Int!

  """ค่าความดันตัวล่าง"""
  diastolic: Int!

  """อัตราการเต้นของหัวใจ"""
  pulse: Int!

  """ความมั่นใจของ AI"""
  confidence: Float
}

type UserType {
  id: String!
  email: String
  firstname: String!
  lastname: String!
  phone: String!
  avatar: String
  role: String!
  createdAt: DateTime!
  dob: DateTime
  gender: String
  weight: Float
  height: Float
  congenitalDisease: String
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type AuthPayload {
  token: String!
  user: UserType!
}

type SessionType {
  id: String!
  deviceLabel: String
  userAgent: String
  isActive: Boolean!
  revokedAt: DateTime
  lastActiveAt: DateTime!
  createdAt: DateTime!
}

type ReadingType {
  id: Int!
  userId: String!
  clientId: String
  systolic: Int!
  diastolic: Int!
  pulse: Int!
  status: String!
  measuredAt: DateTime!
  imageUri: String
  notes: String
  createdAt: DateTime!
}

type PostType {
  id: Int!
  userId: String!
  clientId: String
  userName: String!
  userAvatar: String
  content: String!
  category: String!
  likes: Int!
  comments: Int!
  createdAt: DateTime!
  updatedAt: DateTime
  isLiked: Boolean!
}

type UploadedImageType {
  key: String!
  url: String!
  bucket: String!
  imageId: Float
}

type SyncStorageImagesType {
  prefix: String!
  scanned: Float!
  inserted: Float!
  skipped: Float!
}

type CommentType {
  id: Int!
  postId: Int!
  userId: String!
  parentId: Int
  userName: String!
  userAvatar: String
  content: String!
  likes: Int!
  replies: Int!
  isLiked: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime
}

type AlertAnalysisType {
  id: Int!
  systolic: Int!
  diastolic: Int!
  pulse: Int!
  confidence: Float!
  bpLevel: String!
  analysisNote: String
  analyzedAt: DateTime!
  imageUrl: String
}

type AlertType {
  id: Int!
  userId: String!
  analysisId: Int!
  alertMessage: String!
  alertLevel: String!
  isRead: Boolean!
  createdAt: DateTime!
  analysis: AlertAnalysisType
}

type CaregiverLinkType {
  caregiverId: String!
  patientId: String!
  relationship: String!
  caregiverName: String!
  caregiverPhone: String!
  patientName: String!
  patientPhone: String!
}

type Query {
  hello: String!

  """ดึงข้อมูลผู้ใช้ปัจจุบัน"""
  me: UserType!

  """ประวัติการเข้าสู่ระบบ"""
  loginSessions: [SessionType!]!

  """รายการค่าความดันของผู้ใช้"""
  readings(limit: Int! = 200, offset: Int! = 0): [ReadingType!]!

  """รายการโพสต์ชุมชน (สาธารณะ)"""
  posts(category: String, limit: Int! = 100, offset: Int! = 0): [PostType!]!

  """รายการความคิดเห็นของโพสต์"""
  postComments(postId: Int!, parentId: Int): [CommentType!]!

  """รายการแจ้งเตือนของผู้ใช้"""
  alerts(limit: Int! = 100, offset: Int! = 0, unreadOnly: Boolean! = false): [AlertType!]!

  """รายการผู้ดูแล/ผู้ป่วยที่เชื่อมกับบัญชี"""
  caregiverLinks: [CaregiverLinkType!]!
}

type Mutation {
  """วิเคราะห์ภาพความดันโลหิต"""
  analyzeImage(input: AnalyzeImageInput!): BpAnalysisResult!

  """ลงทะเบียนผู้ใช้ใหม่"""
  register(input: RegisterInput!): AuthPayload!

  """เข้าสู่ระบบ"""
  login(input: LoginInput!): AuthPayload!

  """แก้ไขโปรไฟล์"""
  updateProfile(input: UpdateProfileInput!): UserType!

  """เปลี่ยนรหัสผ่านผู้ใช้"""
  changePassword(input: ChangePasswordInput!): Boolean!

  """ออกจากระบบทุกอุปกรณ์"""
  logoutAllDevices: Boolean!

  """ลบข้อมูลผู้ใช้ทั้งหมด"""
  deleteMyData: Boolean!

  """บันทึกค่าความดันโลหิต"""
  createReading(input: CreateReadingInput!): ReadingType!

  """ลบค่าความดัน"""
  deleteReading(id: Int!): Boolean!

  """สร้างโพสต์ใหม่"""
  createPost(input: CreatePostInput!): PostType!

  """แก้ไขโพสต์"""
  updatePost(input: UpdatePostInput!): Boolean!

  """ลบโพสต์"""
  deletePost(id: Int!): Boolean!

  """กดถูกใจ/ยกเลิกถูกใจโพสต์"""
  toggleLike(postId: Int!): Boolean!

  """อัปโหลดรูปโปรไฟล์เข้า S3"""
  uploadProfileImage(input: UploadImageInput!): UploadedImageType!

  """อัปโหลดรูปเครื่องวัดความดันเข้า S3"""
  uploadBloodPressureImage(input: UploadImageInput!): UploadedImageType!

  """ซิงก์รูปเครื่องวัดความดันจาก S3 prefix เข้า table images"""
  syncBloodPressureImagesFromStorage(prefix: String): SyncStorageImagesType!

  """สร้างความคิดเห็นใหม่"""
  createComment(input: CreateCommentInput!): CommentType!

  """แก้ไขความคิดเห็น"""
  updateComment(input: UpdateCommentInput!): CommentType!

  """ลบความคิดเห็น"""
  deleteComment(id: Int!): Boolean!

  """กดถูกใจ/ยกเลิกถูกใจความคิดเห็น"""
  toggleCommentLike(commentId: Int!): Boolean!

  """ทำเครื่องหมายว่าอ่านแจ้งเตือนแล้ว"""
  markAlertRead(id: Int!): Boolean!

  """ทำเครื่องหมายว่าอ่านแจ้งเตือนทั้งหมดแล้ว"""
  markAllAlertsRead: Boolean!

  """เพิ่มผู้ป่วยด้วยเบอร์โทรศัพท์"""
  addCaregiverPatient(patientPhone: String!, relationship: String! = "caregiver"): CaregiverLinkType!

  """ลบความสัมพันธ์ผู้ดูแล/ผู้ป่วย"""
  removeCaregiverPatient(caregiverId: String!, patientId: String!): Boolean!
}

input AnalyzeImageInput {
  """ข้อมูลภาพที่เข้ารหัสเป็น Base64"""
  imageData: String!
}

input RegisterInput {
  firstname: String!
  lastname: String!
  phone: String!
  password: String!
  email: String
  avatar: String
  dob: DateTime
  gender: String
  weight: Float
  height: Float
  congenitalDisease: String
}

input LoginInput {
  phone: String!
  password: String!
  deviceLabel: String
}

input UpdateProfileInput {
  firstname: String
  lastname: String
  phone: String
  email: String
  dob: DateTime
  gender: String
  weight: Float
  height: Float
  congenitalDisease: String
  avatar: String
}

input ChangePasswordInput {
  currentPassword: String!
  newPassword: String!
}

input CreateReadingInput {
  systolic: Int!
  diastolic: Int!
  pulse: Int!
  status: String!
  measuredAt: DateTime!
  clientId: String
  imageUri: String
  notes: String
}

input CreatePostInput {
  content: String!
  category: String!
  clientId: String
}

input UpdatePostInput {
  id: Int!
  content: String
  category: String
}

input UploadImageInput {
  base64: String!
  mimeType: String!
  fileName: String
}

input CreateCommentInput {
  postId: Int!
  content: String!
  parentId: Int
}

input UpdateCommentInput {
  id: Int!
  content: String!
}